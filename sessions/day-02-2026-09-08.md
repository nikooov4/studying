# День 02 — 2026-09-08

- Тема по плану: pointers, pass-by-value, receivers, structs/methods; по просьбе ученика сначала теория дня 1 о массивах и срезах.
- Статус: в процессе.
- Предыдущий отчёт: `day-08-2026-09-08.md` (прежняя трактовка номера дня, заменена явным запросом дня 2).
- Время обучения со слов ученика: не сообщено.

## Этапы

- [x] Сверка прогресса
- [x] Recall
- [ ] Теория и проверка понимания
- [ ] Мини-практика
- [ ] Алгоритмы
- [ ] Backend-практика по плану
- [ ] Review / self-check
- [ ] Итог

## Результаты и ошибки

Согласованный маршрут по текущему запросу: теория дней 1–2 с проверкой каждого текущего этапа до перехода дальше. Решения упражнений пишет ученик. Самоотчёт: алгоритмы «уже почти сделал». Исходные статусы №1–15 сохранены в `2026-09-08-algorithm-baseline.md`; новых проверенных попыток нет, CSV не менялся. По прежнему самоотчёту ученик изучал всю теорию Go и хочет освежить знания. День 1 и ранее начатый recall дня 8 не отмечены завершёнными.

## Оценки затронутых направлений

Не оценено: новых ответов и проверенного кода нет.

## Повторения

Датированных назначений нет. Приоритетные задачи 🟡/🔴 указаны в исходном отчёте алгоритмов; новые попытки не сообщены.

## Точка продолжения

- Текущий этап: сверка прогресса.
- Точное условие незавершённого вопроса: что изменилось с последней записи — что успел повторить из теории дня 1 и какие алгоритмы получил новые статусы 🟢/🟡/🔴? Уже сохранённый список повторять не нужно.
- Что уже сделано: прочитаны инструкции, состояние, темы дней 1–2, трекер и предыдущий отчёт; зафиксирован уточнённый маршрут.
- Что осталось проверить: изменения прогресса со слов ученика, затем понимание тем дня 1.
- Следующий шаг: дождаться сверки, затем один вопрос recall по arrays/slices.

## Продолжение — сверка завершена, recall 1

Самоотчёт: новых статусов алгоритмов нет, действуют только отметки со скриншота; ученик остановился на 3Sum. Завершение задачи не подтверждено, статус не присваивался. Теорию ещё не повторял. CSV без изменений; оценки пока не выставлены.

Текущий этап: recall по дню 1, вопрос 1. Без запуска определить вывод и объяснить причину:

```go
a := [3]int{10, 20, 30}
b := a
b[0] = 99

s := a[:]
s[1] = 77

fmt.Println(a)
fmt.Println(b)
fmt.Println(s)
```

Ответ ожидается. Следующий шаг: проверить вывод и объяснение копирования массива и общего массива у среза; при ошибке дождаться исправления до следующего вопроса. Эта точка продолжения заменяет предыдущую сверку.

## Проверка recall 1 — требуется исправление

Ответ ученика: `a: 10,20,30; b: 99,20,30; s: 77`. Значения b верны, a и s — неверны; объяснение не предоставлено. Код не запускался.

Помощь наставника: объяснение копирования массива при присваивании и того, что `a[:]` охватывает весь массив a без копирования элементов. Запись по индексу среза меняет общий массив; печать среза выводит все его элементы. Полный правильный вывод не выдан.

Актуальная точка ожидания: ученик должен исправить вывод `fmt.Println(a)` и `fmt.Println(s)` и объяснить связь между ними. Следующий вопрос не выдавать до проверки исправления. Пробел для повторения: общий массив у срезов, изменение элемента и печать всего среза. Оценка пока не выставлена.

## Исправление recall 1 — вывод верен, объяснение ожидается

Ученик ответил «10 77 30 оба». Вывод a и s исправлен верно после объяснения наставника: оба `[10 77 30]`. Самостоятельное объяснение причины ещё не получено, вопрос не закрыт.

Актуальная точка ожидания: почему изменение `s[1]` затронуло a, а изменение `b[0]` не затронуло a? После ответа проверить объяснение; к следующему вопросу пока не переходить.

## Recall 1 завершён; recall 2

Ответ ученика: «при b := a мы полностью копируем массив; при s := a[:] мы ссылаемся на массив a». Объяснение верно для данного кода. Первый вопрос завершён после помощи наставника; требуется дальнейшее закрепление общего массива у срезов. Числовая оценка пока не выставлена.

Текущий этап: recall, вопрос 2. Без запуска определить вывод и объяснить значения len и cap:

```go
a := [5]int{10, 20, 30, 40, 50}
s := a[1:3]

fmt.Println(s)
fmt.Println(len(s), cap(s))
```

Ответ ожидается. Следующий шаг: проверить границы среза, длину и ёмкость; при ошибке дать объяснение и дождаться исправления до перехода дальше.

## Проверка recall 2 — требуется исправление

Ответ ученика: `s = 20; 1, 5`. Содержимое среза, len и cap указаны неверно; объяснение не дано. Код не запускался.

Помощь наставника: нижняя граница в `a[low:high]` включена, верхняя исключена. len — число выбранных элементов, cap для такого среза массива — число элементов от начала среза до конца массива. Ёмкость включает доступное место за текущей длиной. Полный правильный вывод не выдан.

Актуальная точка ожидания: заново определить обе строки вывода кода recall 2 и кратко объяснить подсчёт len и cap. Вопрос остаётся незавершённым. Пробелы: границы среза и отличие длины от ёмкости. Оценка пока не выставлена.

## Повторное исправление recall 2

Ответ ученика: `s = 20 30; len(s) = 2; cap(s) = 5`. Содержимое и длина верны после объяснения, cap остаётся неверной.

Дополнительная подсказка: срез начинается с индекса 1; элемент с индексом 0 не учитывается в его ёмкости. Текущий вопрос: сколько позиций от индекса 1 до последнего индекса массива включительно? Ожидается исправление cap; вопрос recall 2 не завершён.

## Recall 2 завершён; recall 3

Ученик ответил «4»: cap исправлена верно после дополнительной подсказки. Recall 2 завершён с помощью наставника; границы и ёмкость среза требуют закрепления. Оценка пока не выставлена.

Текущий вопрос recall 3: без запуска определить вывод и объяснить причину.

```go
a := [5]int{10, 20, 30, 40, 50}
s := a[1:3]
s = append(s, 99)

fmt.Println(a)
fmt.Println(s)
fmt.Println(len(s), cap(s))
```

Ответ ожидается. Проверить понимание append при достаточной ёмкости и изменения общего массива; при ошибке дождаться исправления до перехода дальше.

## Проверка recall 3 — требуется исправить массив

Ответ ученика: `s = 20,30,99; a = 20,30,99,40,50; len(s)=3; cap(s)=4`. Срез, len и cap верны; содержимое a неверно. Объяснение не предоставлено, код не запускался.

Помощь наставника: при достаточной ёмкости append пишет сразу после текущих элементов среза в общий массив без сдвига элементов; начало среза не меняется. Соответствие: s[0] — a[1], s[1] — a[2]. Полный правильный вывод a не выдан.

Актуальная точка ожидания: какому индексу a соответствует новый s[2] и каким будет весь массив a после записи? Recall 3 остаётся незавершённым; дождаться исправления до следующего вопроса.

## Recall 3 — повторная ошибка индекса

Ответ ученика: `a[4]? a = 20,30,40,50,99`. Индекс записи и массив неверны. Сохраняются затруднения с соответствием индексов и отсутствием сдвига элементов при append в пределах ёмкости.

Дополнительное объяснение: начало s находится в a[1]; шаг на одну позицию в s соответствует шагу на одну позицию в a. a[0] остаётся 10; append здесь ничего не сдвигает.

Актуальный вопрос сужен: `s[0] → a[1]`, `s[1] → a[2]`, `s[2] → a[?]` — какой индекс вместо вопросительного знака? Ожидается ответ только об индексе; после проверки вернуться к содержимому a. Полный вывод не выдан, recall 3 не завершён.

## Recall 3 — индекс исправлен

Ответ ученика `a[3]?` верен после подсказок. Осталось проверить содержимое массива.

Актуальный вопрос: исходный a — `[10 20 30 40 50]`; append записывает 99 в a[3], остальные элементы остаются на месте. Что теперь выведет `fmt.Println(a)`? Ответ ожидается; recall 3 пока не завершён.

## Recall 3 — смешение замены и вставки

Ответ ученика: `a = 10,20,30,99,40,50`. Неверно: указаны шесть элементов вместо пяти, выполнена мысленная вставка со сдвигом вместо замены. Индекс записи ранее исправлен верно.

Объяснение наставника: массив [5]int имеет фиксированную длину; запись в ячейку заменяет её значение, без сдвига последующих элементов. Небольшой пример на другом материале: `[7 8 9]` после записи 6 по индексу 1 становится `[7 6 9]`.

Актуальный вопрос: записать массив a после замены значения по индексу 3 на 99, сохранив ровно пять элементов. Полный ответ упражнения не выдан; дождаться исправления. Пробел: замена элемента общего массива при append в пределах ёмкости, в отличие от вставки со сдвигом. Оценка пока не выставлена.

## Recall 3 — содержимое массива исправлено

Ответ ученика: `10 20 30 99 50` — верно после пошаговых подсказок. Теперь все значения вывода определены верно. Самостоятельное объяснение причины использования общего массива ещё не проверено.

Актуальный вопрос: почему в этом примере append использовал исходный массив a, а не создал новый? Объяснить через len и cap среза до добавления. Дождаться ответа до перехода дальше. Recall 3 пока не закрыт; оценки не выставлены.

## Recall 3 — объяснение связи есть, критерий ёмкости не назван

Ученик объяснил обращение через s к участку `[1:3]` массива a, назвав это «переназначением управления». Связь с общим массивом понята, но причина отсутствия выделения нового массива при append не раскрыта.

Уточнение наставника: управление не передаётся, a и s существуют одновременно. append использует исходный массив, когда новая длина не превышает cap; иначе создаёт новый массив.

Актуальный вопрос: до append len(s)=2, cap(s)=4; добавляется один элемент. Какова требуемая новая длина и помещается ли она в текущую ёмкость? Ответ ожидается; recall 3 не завершён.

## Recall 3 завершён; recall 4

Ученик верно указал требуемую длину 3, ёмкость 4 и возможность добавить ещё один элемент без превышения ёмкости. Критерий использования прежнего массива объяснён после подсказок, recall 3 закрыт.

Ученик также предположил, что при нехватке ёмкости cap всегда удваивается, по аналогии с C++ vector. Исправление наставника: append выделяет новый базовый массив и копирует элементы, но точный коэффициент роста не гарантирован; ёмкость должна вместить результат. Старый массив остаётся доступен, если на него продолжают ссылаться. Требует повторения: рост cap не следует считать гарантированным удвоением.

Текущий вопрос recall 4, без запуска:

```go
a := [3]int{10, 20, 30}
s := a[1:3]
s = append(s, 99)
s[0] = 77

fmt.Println(a)
fmt.Println(s)
```

Что выведет код и почему? Точное значение новой cap не спрашивается. Ответ ожидается; проверить понимание нового базового массива при нехватке ёмкости до перехода дальше. Оценка пока не выставлена.

## Проверка recall 4 — требуется исправление связи массивов

Ученик верно проследил s: `[20 30]`, `[20 30 99]`, `[77 30 99]`. Итог s верен. Итог a указан как `[10 77 30 99 50]` — неверно: текущий a имеет тип [3]int, значения 50 в примере нет. Объяснение не предоставлено, код не запускался.

Подсказка наставника: до append len(s)=2 и cap(s)=2, добавление требует длины 3. При нехватке ёмкости append копирует элементы среза в новый массив; присваивание сохраняет срез результата.

Актуальный вопрос: после этого append срез s использует прежний массив a или новый массив — и почему? Дождаться ответа, затем вернуться к итоговому выводу a. Recall 4 не завершён, числовая оценка не выставлена.

## Recall 4 — аргумент и результат append

Ученик предположил, что результат связан со старым s, потому что s передан первым аргументом append. Выявлена путаница между входным значением среза и возвращённым значением.

Объяснение наставника: append получает старое значение среза; при нехватке ёмкости копирует элементы в другой массив и возвращает срез этого массива. Присваивание `s = ...` заменяет значение переменной s результатом. Копирование int не создаёт связь между старыми и новыми ячейками. Полный вывод упражнения не выдан.

Актуальный вопрос: после этого присваивания изменит ли `s[0] = 77` элемент a[1] и почему? Ответ ожидается; recall 4 не завершён.

## Recall 4 — объяснение исправлено

Ответ ученика: «не изменит, потому что копия не ссылается на оригинал». Верно в текущем примере: append выделил отдельный массив и скопировал значения int. Наставник уточняет границу утверждения: простое копирование среза само по себе не создаёт копию элементов.

Актуальный вопрос: заново написать две итоговые строки `fmt.Println(a)` и `fmt.Println(s)` для recall 4. Объяснение исправлено после помощи; итоговый вывод ещё ожидается, следующий вопрос не выдан.

## Recall 4 — добавление ошибочно перенесено в исходный массив

Ответ ученика: `a = 10,20,30,99; s = 77,30,99`. s указан верно, a — неверно: массив [3]int не может получить четвёртый элемент. Несмотря на верное предыдущее объяснение независимости записи, применение правила ещё нестабильно.

Уточнение наставника: 99 добавляется в новый массив, исходный a сохраняет длину 3; дальнейшая запись через s также не меняет a.

Актуальный вопрос: написать только строку вывода `fmt.Println(a)`, учитывая фиксированную длину и отсутствие записей в него в этом примере. Дождаться исправления, recall 4 не завершён. Полный правильный вывод a не выдан.

## Recall 4 завершён; recall 5 — copy

Ответ ученика: `10,20,30` — итог a исправлен верно. Итог s ранее указан верно. Recall 4 завершён после пошаговых подсказок; самостоятельное применение правил append ещё требует закрепления, уверенное владение темой не подтверждено.

Текущий вопрос recall 5, без запуска:

```go
src := []int{4, 5, 6}
dst := make([]int, 2)
n := copy(dst, src)
src[0] = 90

fmt.Println(n)
fmt.Println(dst)
```

Что выведет код и почему? Ответ ожидается. После проверки и исправления ошибок завершить recall и перейти к систематизации теории дня 1 с проверкой понимания. Оценка пока не выставлена.

## Проверка recall 5 — вывод верен, промежуточное состояние требует исправления

Ученик указал исходный src `[4 5 6]`, начальный dst `[0 0]`, n=2; итоговый вывод n=2 и dst `[4 5]` верен. Однако после `src[0] = 90` указал src `[90 4 5]` — неверно, снова предположил сдвиг вместо замены. Объяснение copy пока не дано. Код не запускался.

Подсказка: присваивание по индексу 0 заменяет только этот элемент; элементы с индексами 1 и 2 остаются прежними.

Актуальный вопрос: каким станет src после `src[0] = 90`? Дождаться исправления, затем проверить объяснение результата copy. Recall 5 не завершён; оценка пока не выставлена.

## Recall 5 — промежуточное состояние исправлено

Ответ ученика `[90 5 6]` верен после подсказки. Итог n=2 и dst `[4 5]` ранее указан верно.

Актуальный вопрос: почему copy вернул 2 и почему последующее изменение src[0] не изменило dst[0]? Ожидается объяснение текущего упражнения; recall 5 пока не завершён. После проверки объяснения перейти к систематизации теории дня 1.

## Recall 5 — возвращаемое значение объяснено частично

Ученик верно указал, что copy возвращает число скопированных элементов; независимость dst объяснил тем, что это копия src. Почему скопированы именно два элемента, ещё не объяснил.

Уточнение наставника: отдельный массив для dst создан вызовом make, а copy записывает элементы в уже существующее назначение; сам copy новый массив не создаёт. В данном примере скопированные значения int независимы.

Актуальный вопрос: в src три элемента — почему в этом коде copy скопировал только два? Ожидается объяснение ограничения длиной dst; recall 5 пока не завершён.

## Recall завершён; теория дня 1 — len/cap

Ученик верно объяснил число скопированных элементов: dst создан с двумя элементами. Дополнительно привёл схему make с len=2, cap=4: `0,0,_,_`. Уточнение наставника: это отдельный вариант `make([]int,2,4)`, в исходном `make([]int,2)` обе величины равны 2. Ячейки базового массива за длиной также нулевые, а не неинициализированные. copy ограничен минимумом длин, не ёмкостью, и не расширяет dst.

Все пять вопросов recall завершены, большинство — после подсказок. Обнаруженные пробелы: замена элемента против вставки со сдвигом, соответствие индексов среза и массива, длина и ёмкость, новый массив при append. Устойчивое самостоятельное применение пока не подтверждено.

Текущий этап: теория дня 1, блок len/cap. Срез описывает начало участка базового массива, длину и ёмкость. Индексация ограничена длиной; ёмкость определяет предел расширения без нового массива.

Проверка понимания: `dst := make([]int,2,4)` — допустимо ли сразу выполнить `dst[2] = 7` без append или изменения границ среза и почему? Ответ ожидается; до проверки к следующему блоку не переходить.

## Теория len/cap — ошибка границы индексирования

Ответ ученика: «можно, будет 0 0 7 потому что cap с запасом». Неверно: cap принята за границу доступных индексов.

Объяснение наставника: индекс должен удовлетворять 0 <= i < len(s); присваивание по индексу не расширяет длину. В текущем коде dst[2] вызывает runtime panic при len=2, даже если cap=4. Готовый код исправления не выдан.

Актуальный вопрос: какие индексы доступны сразу после `dst := make([]int,2,4)`? Ответ ожидается; блок len/cap не завершён.

## Индексация исправлена; расширение границ среза

Ответ ученика `0 1` верен после объяснения ограничения индекса длиной.

Теоретическое продолжение: `s = s[:k]` меняет длину на k при k <= cap(s), сохраняя базовый массив; само по себе не изменяет значения элементов. Пример на другом материале: `x := make([]int,1,3); x = x[:2]` даёт len=2, cap=3.

Актуальная проверка понимания, без запуска:

```go
dst := make([]int, 2, 4)
dst = dst[:3]
dst[2] = 7

fmt.Println(dst)
fmt.Println(len(dst), cap(dst))
```

Что выведет код и почему len/cap имеют такие значения? Ответ ожидается; до проверки дальше не переходить. Это заданный фрагмент для анализа после теории, не исправление кода ученика.

## Проверка расширения — значения верны

Ученик верно проследил состояния dst: `[0 0]`, `[0 0 0]`, `[0 0 7]`; итоговый вывод `[0 0 7]` и `3 4` верен. Код не запускался; объяснение пока не предоставлено.

Актуальный вопрос: почему после `dst = dst[:3]` len стал 3, а cap остался 4? Дождаться объяснения до перехода дальше.

## Объяснение расширения — уточнение ёмкости

Ученик объяснил рост длины изменением границ, а сохранение ёмкости — тем, что срез не переполняли. Первая часть верна; вторую уточняет наставник: базовый массив и начало среза остались прежними. Сдвиг начала уменьшает ёмкость и без переполнения.

Теория: для обычного s[low:high] длина равна high-low, ёмкость cap(s)-low. Изменение границ за cap недопустимо и само не выделяет новый массив.

Актуальная проверка: `s := make([]int,3,5); t := s[1:3]` — чему равны len(t) и cap(t), почему? Ответ ожидается; блок len/cap пока не завершён.

## Проверка сдвига начала — повтор границ

Ученик ответил `s = 0 0 0; t = 0`, добавив «потому что так работает срез?». Содержимое s верно, t неверно, len/cap не названы. Наставник напомнил, что нижняя граница включена, верхняя исключена, и спросил входящие индексы.

Ответ ученика `1 2` верен после напоминания. Актуальный вопрос: если t содержит элементы исходного s с индексами 1 и 2, чему равен len(t)? После проверки отдельно вернуться к cap(t). Исходная проверка не завершена; повторяющийся пробел — применение границ среза.

## Длина t определена

Ученик ответил `2` — len(t) верна после пошагового разбора.

Актуальный вопрос: для `s := make([]int,3,5); t := s[1:3]` чему равен cap(t)? Подсказка: базовый массив имеет индексы 0–4, начало t соответствует индексу 1; считать позиции от начала t до конца массива включительно. Ответ ожидается; проверка len/cap не завершена.

## Ёмкость t определена; самостоятельное закрепление

Ответ ученика `4` верен. Пример завершён с подсказками: len(t)=2, cap(t)=4. Из-за повторных ошибок границ требуется самостоятельная проверка на другом материале.

Актуальное упражнение блока len/cap, без запуска:

```go
a := [6]int{10, 20, 30, 40, 50, 60}
s := a[2:5]
```

Указать содержимое s, len(s), cap(s) и кратко объяснить расчёт. Ответ ожидается; блок не считать усвоенным до проверки. Это закрепление текущего блока, переход к следующей теме не выполнен.

## Самостоятельное закрепление — значения верны

Ученик указал `s = 30,40,50; len=3; cap=4`. Все значения верны без дополнительных подсказок к этому примеру. Объяснение пока не предоставлено; код не запускался.

Актуальный вопрос: почему в этом примере длина равна 3, а ёмкость 4? Ожидается краткий расчёт своими словами; после проверки завершить текущий блок len/cap.

## Блок len/cap завершён; копирование значения среза

Ученик верно объяснил длину числом элементов, ёмкость — исключением двух начальных позиций из исходных шести. Уточнение наставника: речь именно о сдвиге начала; уменьшение верхней границы само по себе не уменьшает cap. Последний пример решён и объяснён без дополнительных подсказок к нему. Блок len/cap завершён, повторение из-за прежних ошибок остаётся актуальным.

Теория следующего блока дня 1: значение среза описывает начало участка массива, длину и ёмкость. t := s копирует описание, но не элементы. Изменение длины одного среза не меняет длину другого, при этом массив может оставаться общим.

Актуальная проверка понимания, без запуска:

```go
s := make([]int, 2, 4)
t := s
t = t[:3]
t[0] = 8

fmt.Println(s, len(s))
fmt.Println(t, len(t))
```

Что выведут две строки и почему? Ответ ожидается; дальше не переходить до проверки.

## Копирование значения среза — вывод верен

Ученик верно проследил состояния s и t; итог `s = [8 0], len=2; t = [8 0 0], len=3` верен без дополнительных подсказок к примеру. Объяснение пока не дано, код не запускался.

Актуальный вопрос: почему изменение t[0] видно через s, а увеличение длины t не изменило len(s)? Дождаться объяснения до завершения блока.

## Копирование среза — частичное объяснение

Ученик ответил: «потому что t изменили через срез, открыв ему новый элемент длины». Верно описано расширение t; связь записи в общий элемент с изменением s ещё не объяснена.

Уточнение наставника: t = t[:3] меняет длину только в значении t, у s сохраняется своя длина.

Актуальный вопрос: после t := s элемент t[0] и элемент s[0] находятся в одной ячейке базового массива или в двух отдельных? Дождаться ответа; блок пока не завершён.

## Копирование среза объяснено; мини-практика, пример 1

Ученик ответил, что t[0] и s[0] находятся в одной ячейке, относятся к одному объекту в памяти. Объяснение верно для текущего кода. Блок про копирование значения среза завершён после уточняющего вопроса: описания и длины отдельные, элементы общего массива доступны через оба среза.

Текущий этап: мини-практика дня 1, первый из пяти коротких примеров изменения срезов по плану. Условие:

1. Создать массив из четырёх любых int и срез s первых двух элементов.
2. Скопировать значение s в t.
3. Через append добавить один элемент к t при достаточной ёмкости.
4. Вывести массив, s, t, а также длины и ёмкости обоих срезов.

Прислать самостоятельно написанный Go-код и прогноз вывода до запуска. Готовый код не выдан; локальная запись пока не требуется. Ответ ожидается. После ответа проверить код и прогноз, при ошибке дождаться исправления до следующего примера. Практика целиком и темы дня 2 не завершены.

## Организация кода по просьбе ученика

Созданы `practice/day-01/go.mod`, `practice/day-01/README.md` с условием и `practice/day-01/exercise-01/main.go` с пустой функцией main. Номер папки соответствует теме дня 1; текущее занятие остаётся днём 2. Проверена установленная версия Go: go1.25.6 windows/amd64. Решение не написано и не запускалось.

Точка ожидания прежняя: ученик пишет код примера 1 в указанном main.go и присылает прогноз вывода до запуска. Следующие упражнения не выданы.

## Ревью мини-практики 1 — первая версия

По просьбе «проверяй» прочитан `practice/day-01/exercise-01/main.go`. Код не запускался и наставником не изменялся. Прогноз вывода не предоставлен.

Версия ученика создаёт `num := []int{1,2,3,4}`, затем `s := num[:2]`, объявляет `var t []int`, выполняет `n := copy(t,s)` и `t = append(t,99)`, печатает срезы, длины, ёмкости и n.

Несоответствия условию: num — срез вместо массива; вместо копирования значения s в t используется копирование элементов в nil-срез. В ревью напомнить, что copy не создаёт назначение и не увеличивает его длину. Срез первых двух элементов и вывод присутствуют. Готовые исправления не выданы.

Актуальный вопрос: чему равен len(t) сразу после `var t []int` и сколько элементов сможет скопировать `copy(t,s)`? Дождаться ответа; затем ученик самостоятельно исправляет код и даёт прогноз. Пример 1 не завершён, оценка пока не выставлена.

## Мини-практика 1 — нулевое значение среза

Ученик ответил: «len(t) = сколько угодно. это динамический массив». Неверно: возможность роста смешана с текущей длиной.

Объяснение наставника: `var t []int` объявляет nil-срез с len=0 и cap=0, без базового массива. len всегда конкретное текущее число элементов. append возвращает срез с добавленными элементами; copy длину назначения не увеличивает. Код не изменялся и не запускался.

Актуальный вопрос: при len(t)=0 и len(s)=2 сколько элементов скопирует copy(t,s)? Дождаться ответа, затем вернуться к самостоятельному исправлению кода. Пример 1 не завершён.

## Мини-практика 1 — ответ о copy исправлен

Ответ ученика `0?` верен: в назначение с нулевой длиной скопируется ноль элементов. Ответ получен после объяснения нулевого значения среза.

Актуальное задание: самостоятельно исправить `practice/day-01/exercise-01/main.go` по исходному условию — num должен быть массивом из четырёх int, а t должен получить копию значения среза s с общим базовым массивом. Затем прислать прогноз вывода до запуска. Готовые строки исправления не выданы, код наставником не изменялся и не запускался. Пример 1 остаётся незавершённым.

## Мини-практика 1 — код исправлен

По просьбе «так?» повторно прочитан main.go. Ученик самостоятельно исправил объявление на `num := [4]int{1,2,3,4}`, скопировал значение среза через `t := s`, удалил copy и n. Остались s первых двух элементов, append одного элемента к t и вывод всех требуемых значений. По чтению код соответствует условию. Наставник код не менял и не запускал.

Актуальная точка ожидания: прогноз полной строки fmt.Println в порядке `num, s, t, len(num), cap(num), len(s), cap(s), len(t), cap(t)` до запуска. Затем проверить прогноз и объяснение и выполнить проверку запуском. Пример 1 пока не завершён.

## Мини-практика 1 — прогноз требует исправления

Прогноз ученика: `num=[1 2 3 4], s=[1 2], t=[1 2 99]; len/cap num=4/4, s=2/2, t=3/3`. Содержимое s/t, все длины и cap(num) верны. Ошибки: ёмкости срезов приравнены к их длинам; изменение общего массива num при append не учтено. Код не запускался, полный правильный вывод не выдан.

Актуальный вопрос: после `s := num[:2]` срез начинается с индекса 0 массива из четырёх элементов — чему равен cap(s) и почему? Дождаться исправления; затем вернуться к cap(t) и эффекту append на num. Пример 1 не завершён.

## Мини-практика 1 — граница длины принята за границу ёмкости

Ученик ответил cap=2, объяснив, что [:2] оставляет индексы 0 и 1. Видимый диапазон определён верно, но это длина среза. Верхняя граница среза ошибочно принята за конец базового массива.

Подсказка наставника: [:2] не обрезает сам массив. Схема базового массива `[1 2 | 3 4]`: s видит первые две ячейки, остальные сохраняются за его текущей длиной. Ёмкость считается от начала s до конца базового массива.

Актуальный вопрос: сколько ячеек от начала s до конца num, включая ячейки за текущей длиной? Дождаться исправления cap(s), затем вернуться к cap(t) и num. Код не запускался, пример 1 не завершён.

## Мини-практика 1 — ёмкость s исправлена

Ответ ученика `4` верен после схемы: cap(s)=4. len(s)=2 ранее указана верно.

Актуальный вопрос: после t := s у t длина 2 и ёмкость 4; при append одного элемента нужна длина 3. Хватит ли текущей ёмкости или потребуется новый массив? Дождаться ответа, затем вернуться к изменению num и итоговой cap(t). Код не запускался, пример 1 не завершён.

## Мини-практика 1 — ёмкости для append хватает

Ученик верно ответил: «текущей cap хватит». Подтверждено использование прежнего массива; наставник отмечает сохранение ёмкости t.

Актуальный вопрос: каким станет num после `t = append(t,99)` и в какой его индекс запишется 99? Исходный num=[1 2 3 4], t начинается с индекса 0 и до добавления имеет len=2. Дождаться исправления прогноза массива. Код не запускался, пример 1 не завершён.

## Мини-практика 1 завершена; пример 2

Ученик исправил прогноз num на `[1 2 99 4]` — верно. Наставник уточнил индекс записи 2. Код повторно прочитан: изменения ученика только в комментариях. Выполнена проверка запуском. Первый запуск завершился отказом доступа к стандартному кэшу Go; повтор с GOCACHE во временной папке успешен (exit 0).

Фактический вывод: `[1 2 99 4] [1 2] [1 2 99] 4 4 2 4 3 4`. Пример 1 завершён после подсказок и самостоятельного исправления кода. Это проверенный запуск, не самоотчёт. Код наставником не менялся; числовая оценка пока не выставлена. Для повторения остаются cap и влияние append на общий массив.

Текущее задание — пример 2, создан `practice/day-01/exercise-02/main.go` с пустой main, условие добавлено в README:

1. Создать массив из трёх любых int и срез s всего массива.
2. Скопировать значение s в t.
3. Добавить один элемент к t через append, затем изменить t[0].
4. Вывести массив, s, t и длины обоих срезов.

До запуска прислать код, прогноз вывода и объяснение, какие переменные используют общий массив после append. Точную новую cap предсказывать не нужно. Ответ ожидается; практика дня 1 завершена на 1 из 5 примеров, всё занятие и темы дня 2 не завершены.

## Мини-практика 2 — код и прогноз верны

По просьбе «проверь» прочитан `practice/day-01/exercise-02/main.go`. Ученик создал arr=[3]int{1,2,3}, s всего массива, t как копию значения s, добавил 99 к t и изменил t[0] на 77. Код соответствует условию, прогноз промежуточных состояний и полного вывода записан в комментариях и полностью верен без дополнительных подсказок к упражнению.

Код проверен запуском `go run ./exercise-02` с GOCACHE во временной папке, exit 0. Вывод: `[1 2 3] [1 2 3] [77 2 3 99] 3 4`. Код наставником не изменялся.

Актуальный вопрос: почему после append изменение t[0] не затронуло arr и s? Объяснить через len/cap до добавления и базовые массивы после него. Объяснение ожидается; пример 2 пока не завершён, следующий пример не выдан.

## Мини-практика 2 — требуется условие выделения массива

Ученик ответил: «потому что append создает новую копию старого t. len(t) стал больше потому что добавили новый элемент». Рост длины объяснён верно; создание нового массива верно для текущего примера, но условие нехватки ёмкости не названо. Не считать подтверждённым понимание того, что append копирует не всегда.

Уточнение наставника: при нехватке ёмкости создаётся новый базовый массив и копируются элементы; при достаточной ёмкости используется прежний массив.

Актуальный вопрос: какие len(t) и cap(t) были до append и какая длина потребовалась после добавления 99? Дождаться ответа и проверить условие; пример 2 пока не завершён.

## Мини-практика 2 — новая ёмкость не обязана равняться длине

Ответ ученика: «до 3 и 3. после 4 и 4». Исходные len/cap=3/3 и новая len=4 верны. Утверждение о новой cap=4 не гарантируется. Требуемая длина превышает старую ёмкость, что объясняет новый массив.

Наставник объясняет: cap после append достаточна для новой длины, но может быть больше; точное значение нельзя вывести как равное len. Дополнительный запуск для измерения cap не выполнялся.

Актуальный вопрос: какое утверждение гарантировано после этого append — cap(t)==4 или cap(t)>=4, и почему? Ответ ожидается; пример 2 пока не завершён.

## Мини-практика 2 завершена; пример 3

Ученик верно выбрал cap(t)>=4 и объяснил, что точный размер выделения не гарантирован. Наставник уточняет нижнюю границу: ёмкости должно хватать для всех элементов. Пример 2 завершён: код и прогноз самостоятельно верны и проверены запуском, объяснение потребовало уточнений про нехватку ёмкости и её рост. Завершены 2 из 5 примеров дня 1; занятие не завершено.

Создан `practice/day-01/exercise-03/main.go` с пустой main; условие добавлено в README. Текущее задание:

1. Создать src из трёх разных int и отдельный dst через make с длиной 2, ёмкостью 5.
2. Скопировать элементы src в dst через copy и сохранить число скопированных элементов.
3. Изменить src[0].
4. Вывести src, dst, результат copy, len(dst) и cap(dst).

До запуска прислать прогноз (можно в комментариях) и объяснение, чем ограничено число копируемых элементов. Ответ ожидается, готовый код не выдан. Пример 3 не запускался.
