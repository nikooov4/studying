# День 02 — 2026-09-11

- Тема по плану: pointers, pass-by-value, structs/methods, receivers.
- Статус: завершён по явному запросу ученика 11.09.2026; алгоритмы не проверены и выполненными не отмечены.
- Предыдущий отчёт: `day-02-2026-09-10.md`.
- Время обучения со слов ученика: не сообщено.
- Прогресс дня 3 из другого диалога сохранён без изменений.
- Git: рабочее дерево содержит локальные изменения. Fetch origin не удался: `Recv failure: Connection was reset`. Pull, commit и push не выполнялись; удалённое состояние не подтверждено.

## Этапы

- [x] Сверка прогресса — ранее в учебном дне.
- [x] Recall — ранее, с подсказками.
- [x] Теория и проверка понимания — основные блоки разобраны; устойчивость понимания проверить практикой.
- [x] Мини-практика — все 5 примеров дня 2 завершены, с исправлениями по ревью.
- [ ] Алгоритмы
- [x] Практика структур по плану — включена в примеры 4–5; отдельного backend-проекта в плане дня 2 нет.
- [x] Review / self-check
- [x] Итог теории и практики; алгоритмическая часть остаётся открытой.

## Результаты и ошибки

Ученик верно ответил `8` для Counter с pointer receiver и объяснил обращение через ссылку. Наставник уточняет: receiver получает копию адреса, через который меняет исходную структуру; передача остаётся по значению. Ответ верен без дополнительных подсказок, код не запускался.

Основные блоки теории дня 2 рассмотрены: указатели, копирование аргументов, переназначение указателя, структуры, value/pointer receivers. Ранее были ошибки при различении копии значения и общего адреса; практику и итоговую проверку не считать выполненными.

## Оценки затронутых направлений

Не оценено численно. Последние ответы о receivers верны самостоятельно, устойчивость применения пока не проверена кодом.

## Повторения

- Копирование int, массива, значения среза и указателя.
- Изменение адреса в указателе против записи по адресу.
- Сохранены повторы len/cap и copy/append из предыдущих отчётов.

## Точка продолжения

- Текущий этап: практика дня 2, пример 1 из 5.
- Созданы `practice/day-02/go.mod`, README с условием и `exercise-01/main.go` с пустой main.
- Условие: написать две функции без результата. Первая принимает int и увеличивает параметр на 5; вторая принимает *int и увеличивает число по адресу на 5. В main создать число 10, вызвать первую функцию и вывести число, затем вторую и снова вывести число.
- До запуска записать прогноз вывода и объяснить различие результатов.
- Следующий шаг: дождаться самостоятельного кода и прогноза, проверить до следующего задания. Готовое решение не выдано.

## Пример 1 завершён; соединение с GitHub проверено

Ученик написал PlusFive с параметром int и PlusFiveAdress с *int. Код соответствует условию; комментарии верно предсказывают 10 после изменения копии и 15 после изменения значения по адресу, объяснение самостоятельное. Запуск `go run ./exercise-01` с временным GOCACHE успешен, вывод `10` и `15`. Код наставником не менялся. Пример 1 из 5 дня 2 завершён.

По просьбе восстановить соединение повторён git fetch origin: успешно. Причина прежнего сброса не установлена, конфигурация Git не менялась. Локальные изменения сохранены, pull при грязном дереве не выполнялся. Commit/push отдельно не запрошены, день не завершён.

Создана пустая main в `practice/day-02/exercise-02/main.go`; условие добавлено в README. Текущее задание: написать функцию Swap с двумя параметрами *int, меняющую местами значения исходных переменных без возвращаемого значения. В main создать две переменные с разными значениями, вызвать Swap и вывести результат. Указатели считать ненулевыми.

До запуска записать прогноз и объяснить, что меняется — адреса в параметрах или значения по адресам. Ответ ожидается; пример 2 не завершён, решение не выдано.

## Проверка Swap — перезаписанное значение

Ученик верно сказал, что меняются значения по адресам. Прочитан exercise-02/main.go: three сохраняет *one, затем *one получает *two, но последняя строка *two=*one использует уже перезаписанное значение. three не читается, что также приводит к ошибке компиляции неиспользованной локальной переменной. Прогноз ученика 2/1 не соответствует текущему коду. Код не запускался и наставником не менялся. В выводе дополнительно присутствуют адреса; их конкретные значения предсказывать не требуется.

Актуальный вопрос: какие значения находятся в three, *one и *two непосредственно перед последней строкой *two=*one при исходных 1 и 2? Дождаться трассировки и самостоятельного исправления; пример 2 не завершён.

## Swap исправлена и проверена запуском

Ученик сообщил three=1, one=2, two=three=1 и исправил код. Наставник уточнил: это итог после последней записи; непосредственно до неё *two ещё равно 2.

Файл перечитан: последняя строка теперь *two=three, обмен корректен. Проверка `go run ./exercise-02` с временным GOCACHE завершилась успешно: значения 1/2 сменились на 2/1, адреса переменных сохранились. Код наставником не менялся.

Актуальная проверка крайнего случая: x:=7; Swap(&x,&x). Чему станет равен x и почему? Ответ ожидается; после проверки завершить пример 2. nil исключён условием; запуск крайнего случая пока не выполнялся.

## Пример 2 завершён; переназначение указателя

Ученик верно объяснил, что x останется 7: меняются значения, а в данном случае оба исходных значения равны 7. Уточнено, что оба параметра указывают на одну ячейку. Крайний случай проверен рассуждением по коду, отдельно не запускался. Пример 2 завершён после самостоятельного исправления с подсказкой; завершены 2 из 5 примеров дня 2.

Создана пустая main в `practice/day-02/exercise-03/main.go`, условие сохранено в README. Текущее задание: написать Redirect с двумя параметрами *int без результата. Внутри переназначить первый параметр так, чтобы он хранил тот же адрес, что второй, затем записать 99 через первый параметр.

В main создать x=10, y=20, p как указатель на x. Передать p и адрес y в Redirect. После вызова вывести x, y и значение по адресу p. До запуска записать прогноз и объяснить, куда указывают p в main и первый параметр функции после переназначения. Ответ ожидается, готовый код не выдан; следующий пример не выдавать до проверки.

## Проверка Redirect — код верен, прогноз неверен

Прочитан exercise-03/main.go: one=two и *one=99 соответствуют условию. В main аргументы переданы правильно. Прогноз `99 99 99` неверен; комментарий `10 20` возле вызова также не описывает состояние после него. Внутренний комментарий верно отмечает общий адрес one и two после переназначения, но two сам остаётся указателем, изменяется значение по адресу.

Подсказка: параметр one получает копию адреса из p; переназначение параметра меняет локальную переменную функции.

Актуальный вопрос: меняет ли one=two саму переменную p в main, и на какую переменную p продолжает указывать? Дождаться объяснения и исправления прогноза. Код не запускался и не исправлялся наставником, пример 3 не завершён.

## Redirect — влияние на x объяснено

Ученик ответил: «раз она меняет копию. значит x не изменится». Вывод верен. Наставник уточняет: скопирован адрес из p, а переназначается локальный one; p в main продолжает хранить адрес x.

Актуальный вопрос: каким будет полный вывод fmt.Println(x,y,*p) после Redirect? Дождаться исправленного прогноза, затем проверить запуском. Пример 3 пока не завершён.

## Redirect — требуется исправить чтение через p

Ученик предсказал `10 99 99`. x и y верны, *p неверно. Подсказка: p в main не переназначался и хранит адрес x; изменение локального one не меняет p.

Актуальный вопрос: p хранит адрес x, а x=10; какое значение прочитает *p? Дождаться исправления последнего значения, затем проверить запуском. Код не запускался, пример 3 не завершён.

## Пример 3 завершён; методы структуры

Ученик исправил прогноз на `10 99 10`. Файл перечитан; код соответствует условию, в комментариях пока остались старые прогнозы `10 20` и `10 99 99`. Наставник просит исправить их самостоятельно. Запуск `go run ./exercise-03` с временным GOCACHE успешен: `10 99 10`. Код наставником не менялся.

Пример 3 завершён после пошаговых подсказок. Для повторения: копия адреса в параметре и неизменность указателя вызывающей функции при локальном переназначении. Завершены 3 из 5 примеров дня 2.

Создана пустая main в `practice/day-02/exercise-04/main.go`, условие добавлено в README. Текущее задание: создать Player с полем Score int, написать AddValue с value receiver и AddPointer с pointer receiver; оба увеличивают Score своего receiver на 5 и ничего не возвращают. В main создать Player со Score=10, вызвать AddValue и вывести Score, затем AddPointer на той же переменной и снова вывести Score.

До запуска записать прогноз и объяснить различие результатов. Ответ ожидается; пример 4 не завершён, готовый код не выдан.

## Проверка Player — условия методов и вывод не выполнены

Прочитан exercise-04/main.go. Создан Player с Score int и начальным значением 10. Реализован один метод AddValue с receiver *Player, который увеличивает Score на 5. По условию у AddValue должен быть value receiver, а второй метод AddPointer отсутствует. В main дважды вызывается AddValue. fmt.Println оба раза без аргументов: печатают пустые строки, а не числа 15/20 из комментариев. Код не запускался и наставником не менялся.

Актуальный вопрос: receiver `(p *Player)` получает копию структуры или копию адреса, и соответствует ли это требуемому value receiver для AddValue? Дождаться ответа, затем самостоятельных исправлений методов, вызовов и вывода. Пример 4 остаётся незавершённым.

## Player — методы исправлены, main ещё требует изменений

Ученик верно ответил, что receiver *Player получает копию адреса. Файл перечитан: AddValue теперь имеет receiver Player, AddPointer — *Player; тела методов соответствуют условию.

В main всё ещё два вызова AddValue и два fmt.Println без аргументов. Прогноз в комментариях 10/15 не соответствует текущей программе. Код наставником не менялся и не запускался.

Актуальное задание: самостоятельно исправить main — после первого метода вызвать второй по условию, передать Score в оба вызова печати и сверить прогноз. Дождаться исправленного файла; затем проверить запуском и объяснение результата. Пример 4 не завершён.

## Player — код и прогноз проверены запуском

Ученик исправил второй вызов на AddPointer и передал p.Score в оба fmt.Println. Код соответствует условию, прогноз 10/15 верен. Запуск go run ./exercise-04 с временным GOCACHE успешен: 10, затем 15. Код наставником не изменялся.

Актуальный вопрос: почему после AddValue Score остался 10, а после AddPointer стал 15? Объяснить, что получает receiver каждого метода. Ответ ожидается; пример 4 пока не завершён, следующий пример не выдан.

## Пример 4 завершён; последний пример дня 2

Ученик верно объяснил, что AddValue получает копию структуры и меняет её, а AddPointer получает копию адреса оригинала и работает с исходной структурой. Проверка объяснения завершена; код и прогноз ранее подтверждены запуском. Завершены 4 из 5 примеров дня 2.

Создана пустая main в `practice/day-02/exercise-05/main.go`, условие добавлено в README. Последний пример объединяет pointer receiver и локальное переназначение указателя:

1. Создать Player с Score int и метод Experiment с pointer receiver без результата.
2. В методе создать отдельный локальный Player со Score=50, переназначить receiver на его адрес, затем через receiver установить Score=99 и вывести это поле.
3. В main создать исходного Player со Score=10, вызвать метод и вывести Score исходного игрока после вызова.

До запуска записать прогноз обеих строк и объяснить, какую структуру меняет метод после переназначения. Это учебный эксперимент, готовый код не выдан. Ответ ожидается; пример 5 и итоговый review не завершены, алгоритмы дня 2 не проверены.

## Проверка примера 5 — результат верен, лишние операции

Прочитан exercise-05/main.go: внутри Experiment создана pp со Score=50, receiver p переназначен на &pp, запись выполнена как `*&p.Score = 99`. Код соответствует условию и компилируется. Прогноз в комментариях 99/10 верен; запуск go run ./exercise-05 с временным GOCACHE подтвердил обе строки, exit 0. Код наставником не менялся.

Замечание ревью: взятие адреса поля и немедленное разыменование в `*&p.Score` избыточны. Актуальный вопрос: что делают & и * в этом выражении и нужны ли они для записи в поле через pointer receiver? Ожидается объяснение и самостоятельное упрощение; пример 5 пока не закрыт.

## Практика завершена; review/self-check

Ученик правильно объяснил, что * и & в текущем выражении можно убрать, а результат обусловлен переназначением receiver внутри функции. Наставник уточнил: pp — отдельно созданная структура, не копия исходного игрока. Файл перечитан: ученик сам упростил строку до p.Score=99. Эквивалентная исходная версия ранее проверена запуском, после этого локального упрощения повторного запуска не было.

Все пять примеров дня 2 завершены. Примеры 2–4 требовали исправлений или подсказок, прогноз последнего примера был верен самостоятельно. Практика изменения структуры через указатель и переназначения локального указателя выполнена. Новых алгоритмических результатов нет, день целиком не закрыт.

Актуальный этап: review/self-check. Текущий вопрос: в каких случаях выберешь pointer receiver, а в каких value receiver? Обосновать выбор. Ответ ожидается; новых упражнений не выдано.

## Self-check — назначение receiver

Ученик выбрал pointer receiver для изменения оригинального объекта, value receiver — для изменения копии; примеров не привёл, предположил тестирование на копии. Критерий изменения оригинала верен, назначение value receiver раскрыто неполно.

Объяснение наставника: value receiver часто применяют для чтения и вычислений над небольшими значениями; изменение копии — следствие семантики, не основная цель. Pointer receiver также позволяет не копировать большую структуру; выбор методов одного типа делают согласованно. Копирование структуры не гарантирует изоляции данных, на которые ссылаются её поля.

Актуальный вопрос: Rectangle содержит только Width и Height типа int, метод Area вычисляет площадь без изменения состояния. Какой receiver выберешь и почему? Ответ ожидается, self-check не завершён.

## Self-check — выбор value receiver обоснован

Ученик выбрал value receiver, объяснив вычисление результата без изменения состояния. Ответ верен для небольшой структуры из двух int; вопрос закрыт. Уточнение: отсутствие изменения состояния само по себе не запрещает pointer receiver, выбор зависит и от размера/семантики типа.

Последний вопрос текущего self-check: каковы Time Complexity и дополнительная Space Complexity написанной учеником Swap с двумя *int и временной int-переменной, почему? Ответ ожидается. После проверки подвести итог согласованного блока теории и практики дней 1–2, не отмечая алгоритмы выполненными без новых отчётов.

## Последний self-check — константная память

Ученик ответил Time O(1), Space O(k), где k — число переменных, равное 3, то есть O(3). Время верно; объяснение памяти требует приведения фиксированной константы к стандартной асимптотической записи. Независимой растущей величины k в данной функции нет.

Объяснение наставника: параметры и временная переменная занимают фиксированную память; Big O описывает рост затрат, а не буквальный подсчёт переменных. Фиксированные положительные константы сводятся к O(1). Память исходных чисел не является дополнительной памятью функции.

Актуальный вопрос: как записать дополнительную Space Complexity Swap в стандартном виде, если вспомогательная память фиксирована? Дождаться исправления, затем итог. Self-check ещё не завершён.

## Итог теории и практики дней 1–2

Ученик ответил O(1) для дополнительной памяти Swap — верно после пояснения о константах. Time O(1) ранее указана верно. Последний self-check завершён, незавершённых вопросов Go в этом диалоге нет.

Выполнены 10 самостоятельных кодовых примеров: пять по массивам/срезам и пять по указателям, передаче по значению и структурам. Каждый пример проверен запуском до закрытия; финальное эквивалентное упрощение записи поля в последнем примере проверено чтением без повторного запуска. Наставник решения не писал и код ученика не исправлял.

Что разобрано: массив как значение, описание среза и общий массив, len/cap, append при достаточной и недостаточной ёмкости, copy и длина назначения; адрес и разыменование, копирование аргументов, обмен значений, переназначение локального указателя, value/pointer receiver и выбор receiver для небольшой структуры.

Пробелы: повторно путались len и cap, копия массива и общий массив среза; изменение локального указателя переносилось на указатель вызывающей функции. Часть прогнозов исправлена поэтапными подсказками, поэтому устойчивое самостоятельное владение не подтверждено во всех темах. Некоторые старые комментарии к коду ещё требуют исправления учеником.

| Направление | Оценка 0–5 | Основание |
|---|---:|---|
| Массивы и срезы | 2 | Повторные ошибки cap и общего массива, правильные ответы после подсказок; один самостоятельный расчёт объяснён верно |
| Указатели и pass-by-value | 3 | Рабочая практика написана учеником; локальное переназначение потребовало пошагового разбора |
| Структуры и receivers | 3 | Различия объяснены верно, последний прогноз самостоятелен; методы и вызовы исправлялись по ревью |
| Big O | 2 | Постоянное время определено верно, постоянная память — после пояснения; более общие случаи не проверены |
| Алгоритмы | Не оценено | Новых отчётов и проверенного алгоритмического кода нет |

Это оценка текущей самостоятельности на разобранном материале, не итоговая готовность к стажировке.

| Когда повторить | Что | Причина | Выполнено |
|---|---|---|---|
| 12.09.2026 | Самостоятельный пример len/cap, копирование массива и среза | Повторяющиеся ошибки | Нет |
| 14.09.2026 | Переназначение локального указателя и receiver | Потребовались подсказки | Нет |
| 18.09.2026 | Смешанный self-check copy/append, pointers, Big O | Проверить удержание и самостоятельность | Нет |

Алгоритмы: новых 🟢/🟡/🔴 не назначено, исходная таблица неизменна, CSV без изменений. День 2 целиком не отмечен завершённым. Прогресс и текущий вопрос дня 3 сохранены отдельно. Следующий шаг для алгоритмической части дня 2 — отчёт об одной попытке с кодом и объяснением, когда ученик вернётся к ней.

Git: повторный fetch ранее успешно проверил связь, ветки совпали (0/0); после этого итог записан локально. Commit/push не выполнялись: полного завершения учебного дня или отдельного указания на отправку не было.

## Завершение дня по запросу ученика

Ученик явно попросил завершить день 2 и сделать коммит изменений. День закрыт как занятие с выполненной теорией, практикой и self-check; алгоритмическая часть остаётся непроверенной, без новых статусов и без отметки о выполнении. Предыдущие записи о незавершённом дне — история до этого запроса.

Итог, оценки и повторения выше актуальны. Следующая точка обучения — уже начатый день 3 и сохранённый вопрос о mmap[v]++. В коммит включаются учебные материалы дней 1–2 и перенесённый промежуточный прогресс дня 3; день 3 не отмечается завершённым. Коммит и push разрешены текущим запросом и правилами AGENTS.md. Проверки кода выполнены в ходе занятия; код ученика и тесты из другого диалога при сохранении не менялись.
