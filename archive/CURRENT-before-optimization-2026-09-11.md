# Текущее состояние обучения

## Актуальная точка — день 3, 11.09.2026

- Статус: в процессе; продолжение проверки понимания мобильной практики. Последний отчёт: `sessions/day-03-2026-09-11.md`, предыдущий: `sessions/day-03-2026-09-10.md`.
- Ученик объяснил чтение и последующую запись при инкременте; после уточняющего вопроса верно назвал zero value отсутствующего ключа: для int это 0. Чтение пустой map безопасно. Вопрос о первом появлении слова закрыт после уточнения.
- Nil map / пустая созданная map: ученик верно предсказал panic при записи в nil map, но ошибочно обобщил запрет на любую пустую map. После объяснения верно ответил: сразу после make длина 0, инкремент разрешён. Проверка завершена с помощью.
- Comma-ok: первоначальный прогноз ученика `0, true, nil, false`; после напоминания о типе int ученик исправил b на `0, zero value`. Затем верно объяснил ok как наличие записи по ключу; термин уточнён до существования ключа независимо от значения. Проверка завершена после помощи с прогнозом.
- Текущий вопрос без запуска: `counts := map[string]int{"go": 2}; delete(counts, "go"); delete(counts, "sql"); value, ok := counts["go"]; fmt.Println(value, ok, len(counts))`. Что выведет код и возникнет ли panic? Объяснить почему. Ответ ожидается; проверяем delete.
- Код: `practice/day-03/exercise-01/main.go`. Новых запусков и изменений кода нет. Алгоритмы ожидаются позже, CSV без изменений; день 3 не завершён.
- Актуальный статус дня 2 из соседнего диалога сохранён: закрыт по явному запросу ученика, алгоритмические задания при этом не проверены. Повторения на 12.09, 14.09, 18.09 сохраняются из отчёта дня 2.

## Актуальная точка после завершения дня 2 — 11.09.2026

- День 2 завершён по явному запросу ученика. Теория, 10 примеров дней 1–2 и self-check выполнены. Алгоритмические задания не проверены и выполненными не отмечены; отчёты о попытках можно добавить позже. CSV и исходные статусы без изменений.
- Итог дня 2: `sessions/day-02-2026-09-11.md`. Оценки и повторения 12.09, 14.09, 18.09 сохранены в итоговом разделе отчёта; текущих заданий Go дня 2 нет.
- Продолжение обучения: уже начатый день 3, статус «в процессе». Отчёт: `sessions/day-03-2026-09-10.md`, код: `practice/day-03/`. Текущий вопрос: почему в wordFrequency выражение mmap[v]++ работает при первом появлении слова, когда ключа ещё нет? Ответ ожидается; следующее задание пока не выдавать.
- По просьбе сделать коммит подготовлено сохранение учебных изменений дней 1–2 и перенесённого промежуточного прогресса дня 3. День 3 не закрыт. Код ученика не исправлялся; старые комментарии-прогнозы сохраняются как есть.
- Все записи ниже — история прежних точек ожидания, заменённая этим разделом там, где они противоречат текущему статусу.

## Итог диалога дней 1–2 — 11.09.2026

- Согласованный блок теории и практики дней 1–2 завершён: 5 примеров массивов/срезов и 5 примеров указателей/структур написаны учеником, проверены и разобраны. Self-check завершён: Space Complexity Swap исправлена на O(1) после объяснения; Time O(1) ранее верна.
- День 2 полностью не закрыт: алгоритмы не проверены, новых попыток и статусов нет. Незавершённого Go-вопроса в этом диалоге нет. Текущий вопрос дня 3 из другого диалога ниже сохранён.
- Последний итог этого диалога: `sessions/day-02-2026-09-11.md`. Код: `practice/day-01/exercise-01/`–`exercise-05/`, `practice/day-02/exercise-01/`–`exercise-05/`. Комментарии-прогнозы в некоторых предыдущих файлах могут содержать старые ошибки; исправляет ученик.
- Оценки по проверенным ответам: массивы/срезы 2/5 (повторные ошибки cap и копирования, исправлены с подсказками); указатели/pass-by-value 3/5 (практика выполнена, переназначение потребовало подсказок); структуры/receivers 3/5 (объяснение различий верное, код исправлен по ревью); Big O 2/5 (время верно, постоянная память потребовала пояснения). Алгоритмы по коду не оценены.
- Повторения: 12.09 — короткий самостоятельный пример на len/cap и копирование массива/среза; 14.09 — локальное переназначение указателя и receiver; 18.09 — смешанный self-check с copy/append и оценкой сложности. Даты — назначения, не выполненные повторы; учебный день автоматически не менять.
- Следующий шаг по дню 2: получить отчёт об одной алгоритмической попытке в соответствующем диалоге либо повторение Go по очереди. По дню 3 сохраняется ожидание ответа о mmap[v]++.
- Git: последнее fetch успешно, проверка веток показала 0/0. Итог и учебные изменения пока локальны; commit/push не выполнялись, поскольку учебный день не завершён и отдельного запроса на отправку не было.

## Продолжение диалога дня 2 — 11.09.2026

- Последний self-check Swap: ученик верно указал Time O(1); Space обозначил O(k), где k=3 переменные, затем O(3). Требуется нормализация константы и понимание зависимости от входа.
- Объяснение: число параметров и временных переменных Swap фиксировано, не зависит от размера входа; фиксированные константы в Big O сводятся к O(1). Сами исходные числа не считаются новой вспомогательной памятью функции.
- Актуальный вопрос: как записать дополнительную Space Complexity Swap в стандартном виде, если объём вспомогательной памяти фиксирован? Дождаться исправления, затем итог теории/практики. Self-check пока не завершён.

- Self-check Rectangle.Area: ученик верно выбрал value receiver, поскольку метод вычисляет результат без изменения состояния. Выбор обоснован для небольшой структуры из двух int; вопрос закрыт.
- Последний вопрос текущего self-check: каковы Time Complexity и дополнительная Space Complexity написанной Swap с двумя *int и временной int-переменной, почему? Ответ ожидается. После проверки подвести итог согласованного блока теории/практики дней 1–2; алгоритмы без новых отчётов не считать выполненными.

- Self-check receivers: ученик верно выбрал pointer receiver для изменения оригинала; value receiver связал с изменением копии и предположил использование для тестирования, примеров не привёл. Ответ частичный.
- Уточнение: value receiver часто используют для чтения и вычислений над небольшими значениями; изменение копии не является его основной целью. Pointer receiver также позволяет избежать копирования большой структуры; для методов одного типа важна согласованность. Копия структуры не гарантирует изоляцию данных в её полях-срезах/указателях.
- Актуальный вопрос: Rectangle содержит только Width и Height типа int; метод Area вычисляет площадь, не меняя состояние. Какой receiver выберешь и почему? Дождаться ответа, self-check не завершён.

- Пример 5 завершён: ученик правильно объяснил избыточность *& и переназначение receiver на локальную структуру. Уточнено: pp — новая отдельная структура, не копия исходной. Файл перечитан, ученик сам заменил выражение на p.Score=99. Эквивалентная исходная версия ранее проверена запуском; после упрощения повторный запуск не выполнялся.
- Все 5 примеров дня 2 завершены; практика изменения struct через pointer и локального переназначения выполнена. День целиком не завершён: review/self-check и алгоритмы не закрыты.
- Актуальный этап этого диалога: review/self-check. Вопрос: в каких случаях выберешь pointer receiver, а в каких value receiver? Обосновать выбор. Ответ ожидается. Точка дня 3 ниже сохранена.

- Пример 5: код соответствует условию, прогноз 99/10 верен. Запуск успешен: 99, затем 10. Receiver переназначен на локальную pp, исходная структура не меняется. Код наставником не изменялся.
- Замечание ревью: `*&p.Score = 99` допустимо, но содержит лишние взятие адреса и разыменование. Актуальный вопрос: что делают & и * в этом выражении и нужны ли они для записи в поле через pointer receiver? Дождаться объяснения и самостоятельного упрощения. Пример 5 пока не закрыт.

- Пример 4 завершён: ученик правильно объяснил копию структуры у AddValue и копию адреса исходной структуры у AddPointer. Код и прогноз ранее проверены запуском. Завершены 4 из 5 примеров дня 2.
- Актуальный пример 5: создан `practice/day-02/exercise-05/main.go` с пустой main. Player с Score int, метод Experiment с pointer receiver без результата: создать отдельный локальный Player со Score=50, переназначить receiver на его адрес, затем через receiver установить Score=99 и вывести поле. В main исходный Player со Score=10, вызов метода и вывод исходного Score. До запуска прогноз обеих строк и объяснение, какую структуру меняет метод. Ответ ожидается; день 2 не завершён, итоговый review и алгоритмы ещё не закрыты.

- Пример 4: исправлены вызов AddPointer и оба вывода p.Score. Код и прогноз 10/15 верны. Запуск go run ./exercise-04 успешен: 10, затем 15. Код наставником не менялся.
- Актуальный вопрос: почему после AddValue Score остался 10, а после AddPointer стал 15? Объяснить, что получает receiver каждого метода. Ответ ожидается; пример 4 пока не завершён.

- Пример 4: ученик верно объяснил, что *Player receiver получает копию адреса. В коде теперь правильно объявлены AddValue с Player и AddPointer с *Player. Однако main по-прежнему дважды вызывает AddValue, а fmt.Println оба раза без аргументов. Прогноз 10/15 не соответствует текущему коду. Код не запускался и наставником не изменялся.
- Актуальное задание: самостоятельно исправить main — после первого метода вызвать второй по условию и передать Score в оба вызова печати; сверить прогноз. Дождаться исправленного файла, затем проверить запуском и объяснение. Пример 4 не завершён.

- Пример 4 Player: прочитан код. Реализован только AddValue с receiver *Player (указатель вместо требуемого значения), AddPointer отсутствует, дважды вызывается AddValue. Оба fmt.Println вызваны без аргументов, поэтому прогноз чисел 15/20 не соответствует печати. Код не запускался и не изменялся наставником.
- Актуальный вопрос: receiver `(p *Player)` получает копию структуры или копию адреса, и соответствует ли это требуемому value receiver для AddValue? Дождаться ответа, затем ученик исправляет методы, вызовы и вывод. Пример 4 не завершён.

- Пример 3 завершён с подсказками: ученик исправил полный прогноз на `10 99 10`, запуск подтвердил результат (exit 0). Код наставником не менялся. В комментариях файла остались старые прогнозы — ученик исправляет сам. Различие локального указателя и p в main требует повторения. Завершены 3 из 5 примеров дня 2.
- Текущий пример 4: `practice/day-02/exercise-04/main.go` (пустая main). Структура Player с Score int; методы AddValue с value receiver и AddPointer с pointer receiver, каждый прибавляет 5 к Score своего receiver, ничего не возвращает. В main Player со Score=10, вызвать AddValue и вывести Score, затем AddPointer на той же переменной и снова вывести Score. До запуска прогноз и объяснение. Ответ ожидается.

- Redirect: прогноз `10 99 99` частично верен — x и y определены правильно, *p неверно. Сохраняется смешение локального one и указателя p вызывающей функции.
- Актуальный вопрос: p в main по-прежнему хранит адрес x, а x=10; какое значение прочитает *p? Дождаться исправления последнего значения, затем проверить запуском. Код не запускался, пример 3 не завершён.

- Redirect: ученик верно заключил, что x не изменится, поскольку меняется копия. Уточнено: копируется адрес из p, переназначается локальный one; p в main сохраняет адрес x.
- Актуальный вопрос: каким будет полный вывод fmt.Println(x,y,*p) после Redirect? Дождаться исправленного прогноза, затем проверить запуском. Пример 3 не завершён.

- Пример 3: Redirect написана по условию — one=two, затем *one=99. Прогноз `99 99 99` неверен; комментарий `10 20` после вызова тоже не соответствует состоянию. Комментарий внутри функции верно связывает one и two с одним адресом, но two остаётся указателем, число хранится по адресу.
- Актуальный вопрос: параметр one получает копию адреса из p; меняет ли one=two саму переменную p в main, и на какую переменную p продолжает указывать? Дождаться объяснения и исправления прогноза. Код не запускался и наставником не менялся, пример 3 не завершён.

- Пример 2 завершён: ученик верно объяснил крайний случай Swap(&x,&x) — x остаётся 7, оба параметра указывают на одну ячейку. Обычный случай ранее проверен запуском, крайний — по коду и объяснению. Завершены 2 из 5 примеров дня 2.
- Актуальный пример 3: `practice/day-02/exercise-03/main.go`, создана пустая main. Написать Redirect с двумя *int без результата: переназначить первый параметр на адрес из второго, затем записать 99 через первый. В main x=10, y=20, p указывает на x; передать p и адрес y, вывести x, y и значение по адресу p. До запуска прогноз и объяснение, куда указывают p в main и локальный первый параметр после переназначения. Ответ ожидается; готовый код не выдан.

- Пример 2 Swap: ученик самостоятельно исправил последнюю запись на *two=three. Код прочитан и проверен запуском: значения 1/2 стали 2/1, адреса переменных не изменились; exit 0. Объяснение ученика описывало состояние после последней записи, что уточнено. Код наставником не менялся.
- Актуальный вопрос на крайний случай: x:=7; Swap(&x,&x). Чему станет равен x и почему? Ответ ожидается, затем проверить объяснение и завершить пример 2. Сложность можно оценить в review; nil исключён условием.

- Пример 2 Swap: ученик верно объяснил изменение значений по адресам. В коде сохранено three=*one, затем *one=*two, но последняя запись *two=*one читает уже перезаписанное значение; three ни разу не читается. Прогноз обмена 2/1 не соответствует коду. Неиспользованная локальная three также мешает компиляции. Код не запускался и не исправлялся наставником.
- Актуальный вопрос: какие значения в three, *one и *two непосредственно перед последней строкой *two=*one при начальных 1 и 2? Дождаться трассировки, затем ученик исправляет код сам. Пример 2 не завершён. Прогноз адресов не требуется; в fmt.Println адреса также выводятся.

- Актуальная проверка Git по просьбе пользователя: повторный fetch origin успешен, соединение работает. Предыдущий сброс был временным; конфигурация Git не менялась. Отправка изменений отдельно не запрошена, день не завершён.
- Практика дня 2, пример 1 завершён: функции с int и *int написаны верно, прогноз и объяснение в комментариях верны самостоятельно. Запуск успешен: `10`, затем `15`. Код наставником не изменялся.
- Текущий пример 2 из 5: в `practice/day-02/exercise-02/main.go` написать Swap с двумя *int, меняющую местами значения исходных переменных без возвращаемого значения. В main две разные переменные, вызов и вывод. До запуска записать прогноз и объяснить, меняются адреса в параметрах или значения по адресам. Указатели считать ненулевыми. Пустая main создана, ответ ожидается; остальные задания пока не выдавать.

- Git 11.09: fetch origin не удался из-за сброса соединения, удалённое состояние не подтверждено. Локальные изменения сохранены; pull/commit/push не выполнялись.

- Ответ на pointer receiver: `8`, потому что доступ идёт через адрес исходной структуры. Верно без подсказок; уточнено, что receiver получает копию указателя, передача остаётся по значению. Код не запускался.
- Актуальный этап этого диалога: практика дня 2, пример 1 из 5. Созданы `practice/day-02/go.mod`, README и пустая main в `practice/day-02/exercise-01/main.go`.
- Задание: две функции без результата — одна принимает int и увеличивает параметр на 5, другая принимает *int и увеличивает число по адресу на 5. В main число 10; вызвать первую и вывести число, затем вторую и вывести число. До запуска записать прогноз и объяснение различий. Ответ ожидается; готовый код не выдан.
- Последний отчёт дня 2: `sessions/day-02-2026-09-11.md`. Точка дня 3 ниже сохранена; оба дня не отмечены завершёнными. Это дополнение заменяет прежнюю точку ожидания дня 2.

## Дополнение из диалога дня 2 — 10.09.2026

- Точка дня 3 ниже сохранена. В диалоге дня 2 ученик ответил `7` для Counter с value receiver и верно объяснил работу с копией. Этот прежний вопрос закрыт без дополнительных подсказок.
- Текущий вопрос дня 2: заменить receiver `(c Counter)` на `(c *Counter)` в примере Counter{N:7}, сохранив `c.N++` и вызов `counter.Inc()`. Что выведет counter.N и почему? Объяснено, что pointer receiver получает копию адреса структуры. Ответ ожидается.
- Отчёт этого продолжения: `sessions/day-02-2026-09-10.md`. День 2 не завершён, практика дня 2 ещё впереди. Это дополнение заменяет только старую точку ожидания дня 2, а не текущий вопрос дня 3.

## Актуальная точка — день 3, 10.09.2026

- Статус: в процессе. По явному запросу ученика продолжаем день 3, начатый с телефона; день 2 остаётся незавершённым. Старые записи ниже — история, а не текущие задания.
- Последний отчёт: `sessions/day-03-2026-09-10.md`; исходный мобильный отчёт: `sessions/day-03-2026-09-10-mobile.md`.
- С телефона перенесены recall, теория map/strings, три упражнения и предварительный review. Устные ответы и оценки известны только из переданного отчёта.
- Код ученика перенесён в `practice/day-03/exercise-01/main.go`, `exercise-02/main.go`, `exercise-03/main.go`. Наставник добавил проверочные тесты: все три пакета и 11 случаев прошли `go test -v ./...`. Решения функций не исправлялись.
- Текущий этап: короткая проверка понимания перенесённой практики, один вопрос. Почему в твоей wordFrequency выражение `mmap[v]++` работает при первом появлении слова, когда такого ключа ещё нет? Ответ ожидается; дальше не переходить до проверки.
- Алгоритмы ученик пришлёт позже; новых завершённых попыток нет, CSV без изменений. По дню 3 нужно 4–5 задач согласно плану. День не закрыт.
- Повторения: nil map и make, comma-ok, byte/rune и байтовые индексы, оценка сложности; массивы/slices и len/cap повторены по мобильному отчёту, независимость ответа не подтверждена.
- День 2 на паузе: вопрос про Counter{N:7}, метод Inc с value receiver и вывод counter.N. Ответ ещё ожидается; затем pointer receiver и практика дня 2. Точная история в `sessions/day-02-2026-09-09.md`. В комментарии `practice/day-01/exercise-05/main.go` ученик ещё должен исправить cap=4 на ранее установленное значение; наставник файл не менял.
- Git: рабочее дерево с локальными изменениями дня 2; pull не выполнялся. Fetch не удался из-за сброса соединения. Commit/push пока не выполнялись, дни не завершены.

## Актуальная точка — день 2, 09.09.2026

- Переназначение указателя: ученик верно объяснил, что запись через p меняет other, а x остаётся 10. Проверка завершена после подсказки о цели указателя; код не запускался.
- Актуальный блок: структуры и методы, value receiver. Структура объединяет поля; при копировании структуры копируются значения полей. Receiver связывает метод с типом; receiver типа Counter получает копию структуры, как параметр по значению.
- Текущая проверка без запуска: `type Counter struct { N int }; func (c Counter) Inc() { c.N++ }; func main() { counter := Counter{N:7}; counter.Inc(); fmt.Println(counter.N) }`. Что выведет код и почему? Ответ ожидается; pointer receiver и практика дня 2 ещё впереди.

- Переназначение указателя: ученик предсказал вывод 90 — неверно; при этом верно описал создание other, присваивание его адреса p и изменение переменной по этому адресу. Ошибка — перенос изменения other на x после переназначения p.
- Подсказка: сначала локальный p указывает на x, после p=&other — на other; переназначение p не меняет x. Схема до/после помогает различить адресата записи.
- Актуальный вопрос: какую переменную изменит *p=90 после p=&other — x или other? Дождаться ответа, затем вернуться к выводу fmt.Println(x). Текущий вопрос не завершён.

- Pass-by-value, проверка 1 завершена: ученик верно предсказал `10 50` и объяснил изменение локальной копии a и исходной b через адрес. Уточнена терминология: в обоих случаях передаются значения, во втором — копия адреса b; отдельного механизма передачи по ссылке нет.
- Актуальный блок: переназначение локального указателя. `*p = ...` записывает в указанную ячейку, `p = ...` меняет адрес в локальной переменной p.
- Текущая проверка без запуска: `func change(p *int) { other := 70; p = &other; *p = 90 }; func main() { x := 10; change(&x); fmt.Println(x) }`. Что выведет код и почему? Ответ ожидается, следующий этап не начинать.

- Указатели, проверка 1 завершена после уточнения: ученик верно ответил, что y хранит копию x; принято как копия значения int, не адреса.
- Актуальный блок: pass-by-value. Go копирует значения аргументов в параметры; для int копируется число, для *int — адрес. Через скопированный адрес можно изменить исходную переменную, хотя сам параметр локален.
- Текущий вопрос без запуска: `func change(n int, p *int) { n = 40; *p = 50 }; func main() { a := 10; b := 20; change(a, &b); fmt.Println(a, b) }`. Что выведет код и почему? Ответ ожидается; к следующему блоку не переходить.

- Указатели, объяснение проверки 1: ученик верно описал адрес x в p и запись через него, но ошибочно считает, что y := x также связывает y с той же памятью. Итоговый вывод был верен, модель копирования int требует исправления.
- Объяснение: y := x копирует число в отдельную переменную; p := &x сохраняет адрес x. Для текущего кода схема: y отдельно хранит 10, p указывает на x. Общая ячейка, доступная через два указателя, показывала бы изменение через оба указателя.
- Актуальный вопрос: что хранится в y после y := x — копия числа или адрес x? Дождаться исправления; блок указателей не завершён.

- Указатели, проверка 1: ученик верно проследил x=10, y=10, p как адрес x, изменение x до 25 через *p и итог `[25 10]` (два числа в выводе). Ответ верен без дополнительных подсказок; объяснение независимости y пока не дано. Код не запускался.
- Актуальный вопрос: почему запись через *p изменила x, но не изменила y? Дождаться объяснения; затем перейти к следующему блоку указателей/pass-by-value.

- Пример 5 завершён с подсказками: ученик исправил cap(s) на 3. Запуск успешен: `[1 77 3 4] [99 2 3 4] [77 3] 2 3`. В комментарии main.go пока осталось cap=4, ученик должен поправить сам; наставник код не менял.
- Все 5 примеров практики дня 1 проверены запуском; большинство прогнозов исправлены после помощи. Сильные стороны: самостоятельное написание кода после объяснений, корректные промежуточные состояния в ряде примеров. Пробелы: cap при сдвиге начала, копирование массива против среза, copy ограничен длиной, поведение append. Устойчивое самостоятельное владение пока не подтверждено.
- Повторение на следующем учебном дне (день 3): один самостоятельный пример на массив/срез и len/cap, затем copy/append по результату. Не отмечать весь день 1 или день 2 завершённым; алгоритмы не проверены, день 2 продолжается.
- Актуальный этап: теория дня 2 — указатели. Объяснить: *int — тип указателя на int, &x — адрес x, *p в выражении — значение по адресу; запись через *p изменяет указанную переменную. Указатель также передаётся по значению, подробный разбор позже.
- Текущая проверка без запуска: `x := 10; y := x; p := &x; *p = 25; fmt.Println(x,y)` — что выведет код и почему? Ответ ожидается; к следующему блоку не переходить до проверки.

- Пример 5: ученик верно различил b := a и b := a[:] для массива a. Уточнение: если a сам срез, присваивание также сохраняет общий базовый массив; правило зависит от типа.
- Повторно прочитан main.go: прогноз a исправлен на `[1 77 3 4]` — верно; прогноз cap(s) изменён с 2 на 4 — всё ещё неверно. Остальные прогнозы верны. Код не запускался и наставником не менялся.
- Актуальный вопрос: s := a[1:3] начинается с индекса 1 массива с индексами 0–3; сколько ячеек от начала s до конца массива включительно? Дождаться исправления cap(s) и объяснения, затем проверить запуском. Пример 5 не завершён.

- Пример 5: ответ об общих ячейках a и b и изменении a[0] через b[0] неверен. Путаются копирование массива и копирование значения среза.
- Объяснение: a имеет тип [4]int; присваивание массива копирует все его элементы в отдельный массив. Для срезов присваивание копирует описание и сохраняет общий базовый массив. Небольшой пример на другом материале: массив [2]int{8,9} и его копия независимы при замене int.
- Актуальный вопрос: с учётом отдельного массива b каким будет a после обеих записей b[0]=99 и s[0]=77? Дождаться исправления a; затем проверить cap(s). Код не запускался, пример 5 не завершён.

- Пример 5: код в `practice/day-01/exercise-05/main.go` соответствует условию. Прогноз b=[99 2 3 4], s=[77 3], len(s)=2 верен; a=[99 77 3 4] и cap(s)=2 неверны. Код не запускался и наставником не изменялся.
- Актуальный вопрос: после b := a массивы a и b используют одни и те же ячейки или отдельные, и должно ли b[0]=99 изменить a[0]? Дождаться объяснения и исправления a, затем вернуться к cap(s). Пример 5 не завершён.

- Пример 4 завершён: ученик верно объяснил восстановление видимых элементов сохранением связи s с памятью arr. Уточнено, что изменение границ не стирает элементы массива. Код и исправленный прогноз проверены запуском. Завершены 4 из 5 примеров дня 1.
- Актуальное задание: пример 5 в `practice/day-01/exercise-05/main.go` (пустая main). Создать массив a из четырёх разных int, присваиванием его копию b и срез s двух средних элементов a; изменить первые элементы b и s на разные новые значения; вывести a, b, s, len(s), cap(s). До запуска записать прогноз и объяснить, какие изменения затрагивают a и почему. Ответ ожидается; следующий этап не начат.

- Пример 4: доступ к файлам восстановлен. Прочитан код ученика, он соответствует условию; Markdown-разметки в файле нет. Прогноз был верен, кроме cap=5; ученик исправил cap на 4 в диалоге, в комментарии файла ещё 5. Код наставником не изменялся.
- Проверка запуском успешна: `[1 2 3 99 5] [2 3 99] 3 4`. Актуальный вопрос: почему после уменьшения s до одного элемента и восстановления длины до трёх снова видны прежние значения 3 и 4? Объяснение ожидается; пример 4 пока не завершён.
- Новые правила Git прочитаны из сообщения пользователя. Выполнен fetch origin; локальные изменения main.go сохранены, pull не выполнялся при грязном дереве. Коммит/push в этом продолжении не запрошены, учебный день не завершён.

- Пример 3 завершён после исправлений: ученик объяснил, что copy копирует элементы, но не расширяет назначение. Уточнение: значения в массиве назначения меняются, а длина остаётся прежней; число копируемых элементов — min(len(dst),len(src)). Код и прогноз ранее проверены запуском. Завершены 3 из 5 примеров дня 1.
- Актуальное задание: пример 4 в `practice/day-01/exercise-04/main.go` (пустая main). Создать массив из пяти разных int, s с индексами массива 1–3 включительно; уменьшить длину s до 1, затем восстановить до 3 изменением границ без append/copy; изменить s[2]; вывести массив, s, len(s), cap(s). До запуска записать прогноз s после каждого изменения границ и итогового вывода. Ответ ожидается; это заменяет прежние точки ожидания ниже.

- Повторная проверка примера 3: ученик добавил вывод src и исправил прогноз dst и len. Код и прогноз верны. Запуск `go run ./exercise-03` с временным GOCACHE успешен: `[1 2] [99 2 3] 2 5 2`. Код наставником не изменялся.
- Текущая точка ожидания заменяет вопрос ниже: почему copy скопировал два элемента, хотя cap(dst)=5 и len(src)=3? Ожидается объяснение ограничения длиной назначения; пример 3 пока не завершён.

- Продолжается учебный день 2, номер не изменён по календарю. Последний отчёт: `sessions/day-02-2026-09-09.md`; предыдущая история ниже и в отчёте за 08.09 сохранена.
- Этап: мини-практика дня 1, пример 3 из 5; завершены примеры 1–2. После текущего останутся два примера, затем теория дня 2 по согласованному маршруту. Тема указателей, структур и методов ещё не пройдена.
- Прочитан `practice/day-01/exercise-03/main.go`. Создание src/dst, copy и изменение src[0] выполнены по условию; не выведен src. Прогноз n=2 и cap(dst)=5 верен, но dst=[1 2 3] и len(dst)=3 неверны. Код не запускался и не исправлялся наставником.
- Актуальный вопрос: copy не меняет длину dst; каким будет dst и его len после copy(dst,src) в текущем коде? После исправления прогноза ученик добавляет вывод src, затем проверка и объяснение результата. Пример 3 не завершён.
- Повторения: len против cap, copy ограничен длинами и не расширяет назначение; append с общим и новым массивом. Алгоритмы без новых результатов; CSV без изменений.

## Актуальная точка — день 2, 08.09.2026

### Уточнение прогресса и текущий вопрос

- Мини-практика 2 завершена: ученик верно объяснил гарантию cap(t)>=4 отсутствием гарантии точного размера выделения. Уточнено, что ёмкость должна вмещать все элементы. Код и прогноз ранее проверены запуском; объяснение уточнено с помощью наставника. Завершены 2 из 5 примеров дня 1.
- Актуальное задание: пример 3 в `practice/day-01/exercise-03/main.go` (пустая main). Создать src из трёх разных int и отдельный dst через make с len=2, cap=5; выполнить copy с сохранением результата, изменить src[0]; вывести src, dst, результат copy, len(dst), cap(dst). До запуска прислать прогноз и объяснить ограничение числа копируемых элементов. Ответ ожидается.

- Мини-практика 2: ответ «до 3 и 3, после 4 и 4». Исходные len/cap и новая len верны; новая cap=4 не гарантирована. Требуемая длина 4 превышает старую cap=3, поэтому новый массив необходим.
- Уточнение: после append гарантировано cap(t)>=len(t), точная ёмкость может быть больше требуемой длины; равенство 4 не следует из кода.
- Актуальный вопрос: какое утверждение гарантировано после этого append — cap(t)==4 или cap(t)>=4, и почему? Дождаться ответа; пример 2 пока не завершён.

- Мини-практика 2, объяснение: ученик указал создание копии при append и рост len из-за нового элемента. Для примера направление верное, но не названо условие нехватки ёмкости; фраза может подразумевать, что append всегда копирует.
- Уточнение: при нехватке ёмкости создаётся новый базовый массив и копируются элементы, а не «старый t» как отдельный объект; при достаточной ёмкости массив используется прежний.
- Актуальный вопрос: какие len(t) и cap(t) были до append и какая длина потребовалась после добавления 99? Дождаться чисел и проверки условия, пример 2 пока не завершён.

- Мини-практика 2: код ученика в `practice/day-01/exercise-02/main.go` соответствует условию. Прогноз в комментариях полностью верен без дополнительных подсказок к упражнению. Проверка `go run ./exercise-02` с временным GOCACHE успешна: `[1 2 3] [1 2 3] [77 2 3 99] 3 4`. Код наставником не менялся.
- Актуальный вопрос: почему после append изменение t[0] не затронуло arr и s? Объяснить через len/cap до добавления и базовые массивы после него. Объяснение ожидается; пример 2 пока не завершён.

- Мини-практика 1 завершена с подсказками: ученик исправил num на `[1 2 99 4]`. Код проверен запуском: `[1 2 99 4] [1 2] [1 2 99] 4 4 2 4 3 4`. Первоначально стандартный кэш Go был недоступен; повторный запуск с GOCACHE во временной папке успешен. Код ученика не изменялся.
- Актуальное задание: пример 2 в `practice/day-01/exercise-02/main.go` (создана пустая main). Создать массив из трёх int, s всего массива, t как копию значения s; добавить элемент к t через append и изменить t[0]; вывести массив, s, t и длины срезов. До запуска прислать прогноз и объяснить, какие переменные используют общий массив после append. Точную новую cap не предсказывать.
- Следующий шаг: прочитать код примера 2 и проверить прогноз; при ошибках дождаться исправления. Практика дня 1: завершён 1 из 5 примеров, с помощью; день 2 и занятие не завершены.

- Мини-практика 1: ученик верно ответил, что текущей cap хватает для append одного элемента. Новый массив не нужен; ёмкость t сохраняется.
- Актуальный вопрос: каким станет num после `t = append(t,99)` и в какой его индекс запишется 99? Исходный num=[1 2 3 4], t начинается с индекса 0 и до добавления имеет len=2. Дождаться исправления прогноза массива. Код не запускался, пример 1 не завершён.

- Мини-практика 1: ученик исправил cap(s) на 4 после схемы. len(s)=2 ранее указана верно.
- Актуальный вопрос: после t := s у t длина 2 и ёмкость 4; при append одного элемента нужна длина 3. Хватит ли текущей ёмкости или потребуется новый массив? Дождаться ответа, затем вернуться к изменению num и итоговой cap(t). Код не запускался, пример 1 не завершён.

- Мини-практика 1: ученик объяснил cap=2 тем, что [:2] оставляет индексы 0 и 1. Это расчёт len, не cap; сохраняется смешение верхней границы среза с концом базового массива.
- Подсказка: [:2] не обрезает сам массив; показать базовый массив `[1 2 | 3 4]`, где s видит первые две ячейки, но остальные ячейки сохраняются. Ёмкость считается от начала s до конца базового массива.
- Актуальный вопрос: сколько ячеек от начала s до конца num, включая ячейки за текущей длиной? Дождаться исправления cap(s), затем вернуться к cap(t) и num. Код не запускался, пример 1 не завершён.

- Мини-практика 1, прогноз: `num=[1 2 3 4], s=[1 2], t=[1 2 99]; len/cap num=4/4, s=2/2, t=3/3`. Содержимое s/t, все длины и cap(num) верны; num и ёмкости срезов неверны. Код не запускался.
- Актуальный вопрос: после `s := num[:2]` срез начинается с индекса 0 массива из четырёх элементов — чему равен cap(s) и почему? Дождаться исправления; затем вернуться к cap(t) и эффекту append на num. Пример 1 не завершён.

- Мини-практика 1, повторное ревью: ученик исправил num на `[4]int{1,2,3,4}`, использовал `t := s`, удалил copy и n. Код соответствует условию по чтению; наставник его не изменял и не запускал.
- Актуальная точка ожидания: прогноз полной строки fmt.Println в порядке `num, s, t, len(num), cap(num), len(s), cap(s), len(t), cap(t)` до запуска. Затем проверить прогноз и объяснение, выполнить проверку запуском. Пример 1 пока не завершён.

- Мини-практика 1: ответ `0?` верен — copy в назначение с len=0 копирует ноль элементов. Понимание исправлено после объяснения nil-среза.
- Актуальное задание: самостоятельно исправить `practice/day-01/exercise-01/main.go` по исходному условию — num должен быть массивом из четырёх int, t должен получить копию значения среза s с общим базовым массивом. Затем прислать прогноз вывода до запуска. Готовые строки исправления не выданы; код наставником не изменялся и не запускался. Пример 1 не завершён.

- Мини-практика 1: ответ «len(t) = сколько угодно, это динамический массив» неверен. Путаются текущая длина среза и возможность роста через append.
- Теория: `var t []int` объявляет nil-срез с len=0, cap=0, без базового массива. len всегда конкретное текущее число элементов; append возвращает срез с добавленными элементами, copy длину не увеличивает.
- Актуальный вопрос: при len(t)=0 и len(s)=2 сколько элементов скопирует copy(t,s)? Дождаться ответа, затем вернуться к самостоятельному исправлению кода. Пример 1 не завершён.

- Ревью мини-практики 1: прочитан `practice/day-01/exercise-01/main.go`, не запускался и не изменялся. Ученик создал num через []int (срез вместо массива), объявил nil-срез t и вызвал copy(t,s) вместо копирования значения среза. Прогноз вывода не предоставлен.
- Объяснение ревью: copy копирует элементы в существующее назначение и не увеличивает его длину; копирование значения среза и копирование элементов — разные операции. Выделение s первых двух элементов и вывод присутствуют.
- Актуальный вопрос: чему равен len(t) сразу после `var t []int` и сколько элементов сможет скопировать `copy(t,s)`? Дождаться ответа, затем ученик самостоятельно исправляет несоответствия условию. Пример 1 не завершён.

- По просьбе ученика создана структура для кода: `practice/day-01/go.mod`, `practice/day-01/README.md` с условием и `practice/day-01/exercise-01/main.go` с пустой функцией main. Папка day-01 соответствует теме, занятие остаётся днём 2.
- Актуальная точка ожидания не изменена: ученик пишет пример 1 в `practice/day-01/exercise-01/main.go` и присылает прогноз вывода до запуска. Решение не написано, код упражнения не запускался.

- Копирование среза: ученик верно объяснил, что t[0] и s[0] — одна ячейка общего массива. Вместе с предыдущим объяснением отдельных длин блок завершён после уточняющего вопроса.
- Актуальный этап: мини-практика дня 1, пример 1 из 5 по плану. Самостоятельно написать Go-код: массив из четырёх любых int, срез s первых двух элементов, t как копия значения s; через append добавить один элемент к t при достаточной ёмкости; вывести массив, s и t, длины и ёмкости обоих срезов.
- Точка ожидания: прислать код и прогноз вывода до запуска. Готовый код не выдан. После ответа проверить код и прогноз, при ошибке дождаться исправления; остальные примеры пока не выдавать. Остальные темы и практика дня 2 не завершены.

- Объяснение копирования среза частичное: ученик сказал, что t изменили срезом, открыв ещё один элемент длины. Это верно описывает расширение t, но связь изменения элемента с s не объяснена.
- Уточнение: t = t[:3] меняет длину только в значении t; у s сохраняется своя длина.
- Актуальный вопрос: после t := s элемент t[0] и элемент s[0] находятся в одной ячейке базового массива или в двух отдельных? Дождаться ответа; текущий блок не завершён.

- Копирование значения среза: ученик верно проследил все состояния и указал итог `s = [8 0], len=2; t = [8 0 0], len=3`. Без дополнительных подсказок к примеру; объяснение пока не предоставлено.
- Актуальный вопрос: почему изменение t[0] видно через s, а увеличение длины t не изменило len(s)? Дождаться объяснения до завершения текущего блока.

- Блок len/cap завершён: ученик верно объяснил len=3 числом элементов, cap=4 — сдвигом начала на две позиции при исходной ёмкости 6. Уточнение: вычитается именно сдвиг начала; уменьшение верхней границы само по себе cap не уменьшает. Последний пример решён и объяснён без дополнительных подсказок.
- Актуальный этап: теория дня 1, копирование значения среза. Значение среза описывает начало, len и cap; t := s копирует это описание, но не элементы. Длины переменных могут изменяться независимо при общем массиве.
- Текущая проверка, без запуска: `s := make([]int,2,4); t := s; t = t[:3]; t[0] = 8; fmt.Println(s,len(s)); fmt.Println(t,len(t))`. Что выведут две строки и почему? Ответ ожидается; следующий блок не начинать до проверки.

- Самостоятельное закрепление len/cap: ответ `s = 30,40,50; len=3; cap=4` верен без дополнительных подсказок к этому примеру. Объяснение пока не дано.
- Актуальный вопрос: почему для `a := [6]int{10,20,30,40,50,60}; s := a[2:5]` длина равна 3, а ёмкость 4? Ожидается краткий расчёт своими словами; после проверки завершить текущий блок len/cap.

- Проверка сдвига начала завершена с подсказками: ученик верно указал cap(t)=4; len(t)=2 определена ранее. Требуется самостоятельное закрепление.
- Актуальное упражнение блока len/cap, без запуска: `a := [6]int{10,20,30,40,50,60}; s := a[2:5]`. Указать содержимое s, len(s), cap(s) и кратко объяснить расчёт. Ответ ожидается; блок не считать усвоенным до самостоятельной проверки.

- Проверка сдвига начала: ученик верно указал len(t)=2 после пошагового разбора границ.
- Актуальный вопрос: для `s := make([]int,3,5); t := s[1:3]` чему равен cap(t)? Подсказка: базовый массив имеет индексы 0–4, начало t соответствует индексу 1; считать позиции от начала t до конца массива включительно. Дождаться ответа, проверка len/cap не завершена.

- Проверка `s := make([]int,3,5); t := s[1:3]`: ученик сначала указал `s = 0 0 0; t = 0`, без расчёта len/cap. t неверен; после напоминания о полуоткрытом диапазоне верно назвал входящие индексы `1 2`.
- Актуальный вопрос: если t содержит элементы исходного s с индексами 1 и 2, чему равен len(t)? После проверки отдельно вернуться к cap(t); исходный вопрос пока не завершён.

- Объяснение расширения: ученик верно связал len=3 с изменением границ; сохранение cap объяснил отсутствием переполнения. Уточнено: cap сохраняется в данном случае потому, что базовый массив и начало среза прежние; сдвиг начала может уменьшить cap без переполнения.
- Теория: для обычного двухиндексного выражения s[low:high] длина равна high-low, ёмкость равна cap(s)-low. Расширение границ за cap недопустимо и не создаёт новый массив.
- Актуальная проверка: `s := make([]int,3,5); t := s[1:3]` — чему равны len(t) и cap(t), почему? Ответ ожидается; блок len/cap пока не завершён.

- Проверка расширения среза: ученик верно указал состояния `[0 0]`, `[0 0 0]`, `[0 0 7]` и итог len=3, cap=4. Объяснение пока не предоставлено.
- Актуальный вопрос: почему после `dst = dst[:3]` len стал 3, а cap остался 4? Дождаться объяснения до перехода дальше.

- Проверка индексации: ученик ответил `0 1` — верно после объяснения. Ограничение индекса длиной исправлено.
- Теория: изменение границ `s = s[:k]` может увеличить длину до k <= cap(s) без нового массива; само по себе не меняет значения элементов. Пример на другом материале: `x := make([]int,1,3); x = x[:2]` даёт len=2, cap=3.
- Актуальный вопрос: без запуска определить вывод и объяснить len/cap для `dst := make([]int,2,4); dst = dst[:3]; dst[2] = 7; fmt.Println(dst); fmt.Println(len(dst),cap(dst))`. Ответ ожидается; дальше не переходить до проверки.

- Теория len/cap: ответ «можно, будет 0 0 7 потому что cap с запасом» неверен. Ученик использует cap как границу индексирования.
- Объяснение: индексирование допустимо при 0 <= i < len(s); присваивание по индексу не увеличивает длину. Для переменной dst с len=2 запись dst[2] вызовет runtime panic, несмотря на cap=4.
- Актуальный вопрос: какие индексы доступны сразу после `dst := make([]int,2,4)`? Дождаться исправления, блок len/cap не завершён.

- Recall 5 завершён: ученик верно объяснил ограничение копирования двумя элементами dst. Его схему len=2, cap=4 рассмотрели как отдельный пример; в исходном `make([]int,2)` cap=2. copy копирует min(len(dst),len(src)), не расширяет длину назначения.
- Recall (5 вопросов) завершён с подсказками. Пробелы: замена против вставки, соответствие индексов, len/cap, общий и новый массив при append. Самостоятельное устойчивое применение пока не подтверждено.
- Актуальный этап: теория дня 1, блок len/cap и проверка понимания. Срез описывает начало участка базового массива, длину и ёмкость; индексация ограничена длиной, ёмкость задаёт предел расширения без нового массива. Неиспользуемые ячейки int тоже содержат нули.
- Текущий вопрос: `dst := make([]int,2,4)` — допустимо ли сразу выполнить `dst[2] = 7` без append или изменения границ среза и почему? Ответ ожидается; до проверки дальше не переходить.

- Recall 5: ученик верно назвал результат copy числом скопированных элементов и объяснил независимость dst копированием. Уточнение наставника: отдельный массив dst создан make, copy лишь записывает элементы в назначение и сам не создаёт новый массив.
- Актуальный вопрос: в src три элемента — почему в этом коде copy скопировал только два? Ожидается объяснение ограничения длиной dst. Recall 5 пока не завершён.

- Recall 5: ученик исправил src на `[90 5 6]` — верно после подсказки. Итог n=2 и dst `[4 5]` ранее указан верно.
- Актуальный вопрос: почему copy вернул 2 и почему последующее изменение src[0] не изменило dst[0]? Ожидается объяснение текущего упражнения; recall 5 пока не завершён.

- Recall 5: итоговый вывод `n = 2`, `dst = [4 5]` указан верно. Промежуточный src после `src[0] = 90` указан неверно как `[90 4 5]`; повторяется смешение замены и сдвига. Объяснение copy ещё не предоставлено.
- Актуальная подсказка: присваивание src[0] заменяет только элемент с индексом 0, элементы с индексами 1 и 2 остаются прежними.
- Текущий вопрос: каким станет src после `src[0] = 90`? Дождаться исправления, затем проверить объяснение результата copy. Recall 5 не завершён.

- Recall 4 завершён с пошаговыми подсказками: ученик исправил итог a на `[10 20 30]`; итог s `[77 30 99]` ранее указан верно. Самостоятельное применение правил append требует закрепления.
- Актуальный этап: recall 5, вопрос про copy, ответ ожидается. Без запуска: `src := []int{4,5,6}; dst := make([]int,2); n := copy(dst,src); src[0] = 90; fmt.Println(n); fmt.Println(dst)`. Что выведет код и почему?
- Следующий шаг: проверить ответ, при ошибке дождаться исправления; после завершения recall перейти к систематизации теории дня 1 и проверке понимания. Предыдущие вопросы ожидания ниже неактуальны.

- Recall 4, повтор вывода: ученик указал `a = 10,20,30,99; s = 77,30,99`. s верен, a неверен: добавление ошибочно перенесено и в исходный массив [3]int.
- Уточнение: при нехватке ёмкости 99 добавляется в новый массив; исходный a сохраняет длину 3. После выделения нового массива дальнейшая запись через s также не изменяет a.
- Актуальный вопрос: написать только строку вывода `fmt.Println(a)`, учитывая его фиксированную длину и отсутствие записей в него в этом примере. Дождаться исправления; recall 4 не завершён.

- Recall 4: ученик верно ответил, что `s[0] = 77` не изменит a[1], поскольку копия не ссылается на оригинал. Принято для текущего []int после выделения нового массива; уточнено, что простое копирование среза само по себе не копирует элементы.
- Актуальный вопрос: заново написать две итоговые строки `fmt.Println(a)` и `fmt.Println(s)` для recall 4. Объяснение исправлено после помощи, полный итоговый вывод ещё ожидается. К следующему вопросу не переходить.

- Recall 4: ученик предполагает сохранение связи со старым s, поскольку он передан первым аргументом append. Путает входной срез и возвращённый срез; объяснение требует исправления.
- Объяснение: append получает значение старого среза; при нехватке ёмкости копирует элементы в другой массив и возвращает срез этого массива. Присваивание `s = ...` заменяет значение переменной s возвращённым срезом. Копирование int не связывает старые и новые ячейки.
- Актуальный вопрос: после этого присваивания изменит ли `s[0] = 77` элемент a[1] и почему? Дождаться ответа; recall 4 не завершён.

- Recall 4: промежуточные значения s и итог `[77 30 99]` указаны верно. Итог a указан неверно: `[10 77 30 99 50]`; в текущем коде a имеет тип [3]int, значения 50 нет. Объяснение не дано.
- Подсказка: до append у s длина 2 и ёмкость 2, добавление требует длины 3. При нехватке ёмкости append копирует элементы среза в новый массив; `s = append(...)` сохраняет срез результата.
- Актуальный вопрос: после этого append срез s использует прежний массив a или новый массив — и почему? Дождаться ответа, затем вернуться к итоговому выводу a. Recall 4 не завершён.

- Recall 3 завершён с подсказками: ученик верно объяснил, что требуемая длина 3 помещается в cap=4 и остаётся место ещё для одного элемента.
- Новое заблуждение: cap при нехватке места всегда удваивается. Уточнение: append выделяет новый базовый массив и копирует элементы; конкретный рост ёмкости не гарантирован, гарантируется достаточная ёмкость. Старый массив остаётся доступен через прежние ссылки; аналогия с C++ vector ограничена.
- Актуальный вопрос recall 4, без запуска: `a := [3]int{10,20,30}; s := a[1:3]; s = append(s,99); s[0] = 77; fmt.Println(a); fmt.Println(s)`. Что выведет код и почему? Точное значение новой cap не спрашивается. Дождаться ответа и проверить до перехода дальше.

- Recall 3, объяснение ученика: s обращается к участку `[1:3]` массива a, названо «переназначением управления». Связь с общим массивом описана верно, но критерий использования массива при append не назван.
- Уточнение: управление не передаётся, a и s существуют одновременно; при append исходный массив используется, если новая длина помещается в cap, иначе создаётся новый массив.
- Актуальный вопрос: до append len(s)=2, cap(s)=4; добавляется один элемент. Какова требуемая новая длина и помещается ли она в текущую ёмкость? Дождаться ответа; recall 3 пока не завершён.

- Recall 3: массив исправлен на `[10 20 30 99 50]` — верно после пошаговых подсказок. Все значения вывода теперь определены верно, но самостоятельное объяснение append ещё не проверено.
- Актуальный вопрос: почему в этом примере append использовал исходный массив a, а не создал новый? Объяснить через len и cap среза до добавления. Дождаться объяснения; к следующему вопросу не переходить.

- Recall 3: ответ `a = 10,20,30,99,40,50` неверен: ученик вставил шестой элемент вместо замены значения. Индекс a[3] ранее определён верно.
- Объяснение: длина массива [5]int фиксирована; запись в существующую ячейку заменяет её значение, не сдвигает последующие элементы. Пример на другом материале: `[7 8 9]` после записи 6 по индексу 1 становится `[7 6 9]`.
- Актуальный вопрос: записать массив a после замены значения по индексу 3 на 99, сохранив ровно пять элементов. Полный ответ текущего упражнения не выдан; recall 3 не завершён.

- Recall 3: ученик ответил `a[3]?` — индекс записи определён верно после подсказок.
- Актуальный вопрос: исходный a — `[10 20 30 40 50]`; append записывает 99 в a[3], остальные элементы остаются на месте. Что теперь выведет `fmt.Println(a)`? Дождаться ответа; recall 3 пока не завершён.

- Повтор recall 3: ответ `a[4]? a = 20,30,40,50,99` неверен. Сохраняется путаница между индексами среза и массива, а также предположение о сдвиге элементов.
- Актуальная подсказка: начало s находится в a[1]; переход на одну позицию в s означает переход на одну позицию в a. a[0] остаётся 10, append здесь ничего не сдвигает.
- Текущий вопрос сужен до одного шага: `s[0] → a[1]`, `s[1] → a[2]`, `s[2] → a[?]` — какой индекс вместо вопросительного знака? Полный вывод не выдан; дождаться ответа, затем вернуться к содержимому массива.

- Recall 3: ученик ответил `s = 20,30,99; a = 20,30,99,40,50; len(s)=3; cap(s)=4`. Срез, длина и ёмкость верны, массив a неверен; объяснение не дано.
- Помощь: при достаточной ёмкости append записывает элемент сразу после текущих элементов среза в общий массив, не сдвигает элементы массива и не меняет начало среза. s[0] соответствует a[1], s[1] — a[2].
- Актуальный вопрос: какому индексу a соответствует новый s[2] и каким будет весь массив a после записи? Дождаться исправления, recall 3 не завершён.

- Recall 2 завершён с подсказками: ученик исправил cap на 4; содержимое `[20 30]` и len=2 ранее исправлены верно. Границы и ёмкость требуют закрепления.
- Актуальный этап: recall 3, ответ ожидается. Без запуска: `a := [5]int{10, 20, 30, 40, 50}; s := a[1:3]; s = append(s, 99); fmt.Println(a); fmt.Println(s); fmt.Println(len(s), cap(s))`. Что выведет код и почему?
- Следующий шаг: проверить понимание append при достаточной ёмкости, при ошибке дождаться исправления. Полный ответ не выдан.

- Повторный ответ recall 2: `s = 20 30; len(s) = 2; cap(s) = 5`. Содержимое и длина исправлены верно; ёмкость всё ещё неверна.
- Актуальный вопрос: s начинается с индекса 1, элемент с индексом 0 не входит в его ёмкость; сколько позиций от индекса 1 до последнего индекса массива включительно? Ожидается исправление cap, следующий вопрос не выдавать.

- Recall 2: ответ ученика `s = 20; 1, 5` неверен по содержимому, длине и ёмкости. Вопрос остаётся текущим.
- Помощь: в `a[low:high]` нижняя граница включена, верхняя исключена; len — число выбранных элементов; cap для такого среза массива — число элементов от начала среза до конца массива, включая находящиеся за текущей длиной. Полный вывод не выдан.
- Актуальная точка ожидания: заново определить обе строки вывода кода recall 2 и кратко объяснить подсчёт len и cap. Не переходить дальше до исправления.

- Recall 1 завершён: ученик правильно объяснил, что `b := a` копирует массив, а `s := a[:]` использует массив a. Вывод исправлен после подсказки; объяснение проверено, самостоятельное применение ещё предстоит закрепить.
- Актуальный этап: recall, вопрос 2, ответ ожидается. Без запуска: `a := [5]int{10, 20, 30, 40, 50}; s := a[1:3]; fmt.Println(s); fmt.Println(len(s), cap(s))`. Что выведет код и почему len и cap имеют такие значения?
- Следующий шаг: проверить вывод и объяснение len/cap; при ошибке разобрать её и дождаться исправления. Эта точка заменяет предыдущие вопросы ожидания ниже.

- Исправление recall 1: ученик ответил «10 77 30 оба» — значения a и s теперь верны, после объяснения наставника. Причину своими словами ещё не объяснил; вопрос не закрыт.
- Актуальный вопрос: почему изменение `s[1]` затронуло a, а изменение `b[0]` не затронуло a? Дождаться объяснения, затем проверить его до перехода дальше.

- Проверка recall 1: ученик ответил `a: 10,20,30; b: 99,20,30; s: 77`. Значения b верны; вывод a и s неверен. Объяснение пока не дано.
- Разобрать: массив при присваивании копируется; `a[:]` создаёт срез всего массива a, без копирования элементов; запись `s[1]` меняет элемент общего массива, а печать s показывает весь срез.
- Текущая точка ожидания: исправить вывод `fmt.Println(a)` и `fmt.Println(s)` и объяснить связь между ними. Полный правильный вывод наставник не выдавал. К следующему вопросу не переходить до исправления.
- Пробел для повторения: общий массив у срезов, изменение элемента и печать целого среза. Числовая оценка пока не выставлена.

- Сверка завершена: новых статусов алгоритмов нет, актуальна таблица со скриншота; ученик остановился на 3Sum. Завершение 3Sum не подтверждено, статус не присвоен, CSV без изменений.
- Теорию ученик ещё не повторял; ранее изучал её по самоотчёту.
- Текущий этап: recall по дню 1, вопрос 1; ответ ожидается. Эта точка заменяет вопрос сверки ниже.
- Условие без запуска: `a := [3]int{10, 20, 30}; b := a; b[0] = 99; s := a[:]; s[1] = 77; fmt.Println(a); fmt.Println(b); fmt.Println(s)`. Что выведут три вызова fmt.Println и почему?
- Следующий шаг: проверить ответ; при ошибке объяснить конкретную проблему и дождаться исправления. Далее двигаться по одному вопросу, затем теория дня 1.

- Пользователь явно указал день 2 и маршрут: теория дней 1–2. Эта запись заменяет прежнюю трактовку занятия как дня 8; история ниже сохранена.
- Статус: в процессе; сверка прогресса завершена, текущий этап — recall.
- Теория: сначала arrays/slices, len/cap, append, underlying array, copy; после проверки этого блока — pointers, pass-by-value, receivers, structs/methods.
- Самоотчёт: алгоритмы «уже почти сделал». Исходные статусы №1–15 известны; новые результаты не проверены. Ранее ученик сообщил, что изучал всю теорию Go и хочет освежить её.
- Незавершённый вопрос: что изменилось с последней записи — что успел повторить из теории дня 1 и какие алгоритмы получил новые статусы 🟢/🟡/🔴? Уже сохранённый список повторять не нужно.
- Предыдущий вопрос про GOMAXPROCS отложен при смене маршрута, ответа и оценки нет.
- Последний отчёт: `sessions/day-02-2026-09-08.md`; код текущего задания отсутствует.
- Следующий шаг: после сверки один вопрос recall по arrays/slices; затем теория с проверками по одному вопросу.
- Повторения: датированных назначений нет; приоритеты из `sessions/2026-09-08-algorithm-baseline.md` сохраняются. Завершение дней и практики не подтверждено; оценки не выставлены.

## Исходные статусы алгоритмов

Скриншот таблицы ученика: `sessions/2026-09-08-algorithm-baseline.md`. По №1–15: 3 🟢, 7 🟡, 4 🔴, №15 без статуса. Это самоотчёт, код и объяснения ещё не проверены. Он дополняет прежние записи о неизвестных результатах алгоритмов. Новые отчёты ученик будет присылать в диалог соответствующего дня; сохранять их как отдельные попытки. Текущий этап занятия ниже этим сообщением не изменён.

## Новый запрос — 08.09.2026

- Ученик просит начать день 8 и изучить теорию прошлого и текущего дня; алгоритмы, по самоотчёту, «почти сделал», конкретные результаты не сообщены.
- Есть расхождение с сохранённым ниже состоянием дней 1–2. Переход пока ожидает уточнения; предыдущие дни не отмечены выполненными.
- Текущий этап: сверка номера дня и объёма занятия.
- Текущий вопрос: под днями 7–8 имеются в виду контроль Go Core и начало concurrency или теория за 7–8 сентября (дни 1–2: slices и pointers)?
- Последний отчёт: `sessions/day-08-2026-09-08.md` (номер из запроса, ожидает уточнения).
- Следующий шаг: после уточнения сверить изученные темы и статусы алгоритмов, затем начать текущий этап по одному вопросу.

## Ранее сохранённое состояние

- Статус: рабочее пространство подготовлено; ученик сообщил, что начал алгоритмическую подготовку 07.09.2026. Занятия с наставником ещё не зафиксированы.
- Текущий день плана: по календарю день 2 (08.09); теория дня 1 и дня 2 запланирована на сегодня, выполнение ещё не подтверждено.
- Дата занятия: 08.09.2026 — намечено прохождение теории за 07.09 и 08.09.
- Завершённые дни: нет подтверждённых записей.
- Текущий этап: начальная сверка прогресса.
- Незавершённое задание: нет выданных заданий.
- Прогресс первых 50 алгоритмов: с 07.09.2026 ученик двигался по расписанию, начинающемуся с №9–12. Календарь внесён в раздел 4 `STUDY_PLAN.md`; фактически завершённые задачи, включая №1–8, и статусы пока не сообщены.
- Последний отчёт: отсутствует.
- Код текущего задания: отсутствует.
- Пробелы и оценки: ещё не определены.
- Повторения: ещё не назначены.
- Следующий шаг: уточнить фактический прогресс алгоритмов, затем пройти теорию дня 1 (arrays/slices, len/cap, append, underlying array, copy) с проверкой понимания и закреплением; после выполнения текущего этапа перейти к теории дня 2 (pointers, pass-by-value, receivers, structs/methods). Практику и остальные этапы дней не считать выполненными из-за прохождения теории.

Весь план начинается 07.09.2026. Ученик намерен 08.09 пройти теорию первых двух дней; это намерение, а не подтверждение выполнения. Изменение календаря: `sessions/2026-09-08-plan-update.md`.

Даты в плане — исходный ориентир. Номер дня не продвигается автоматически с календарём.

## Уточнение запроса — текущая точка продолжения

- Текущий день: 8 по исходному запросу ученика; дата занятия: 08.09.2026. Наставник явно обозначил эту трактовку после ответа без уточнения номера.
- Статус: в процессе; сверка изученных тем перед теорией.
- Маршрут по просьбе ученика: теория короткими блоками; повторение Go Core дня 7, затем concurrency дня 8. Контроль дня 7 не считать пройденным. Ранее запланированные дни 1–2 не считать завершёнными.
- Алгоритмы: по самоотчёту часть сделана и разобрана, результаты отмечены в таблице на скриншоте. Скриншот в текущем диалоге недоступен, конкретные статусы не проверены. 3Sum и два повтора ученик отложил; названия повторов не сообщены. CSV без изменений.
- Текущий вопрос: какие из тем slices, maps, pointers, interfaces, errors ученик уже изучал, а какие нужно объяснить с нуля?
- Код текущего задания: не назначен.
- Последний отчёт: sessions/day-08-2026-09-08.md.
- Следующий шаг: по ответу выбрать первый вопрос recall по знакомой теме либо первый теоретический блок, если все темы новые; двигаться по одному вопросу.
- Оценки и повторения: новых оценок и назначений нет.

## Актуальная точка — recall дня 8

- Самоотчёт: ученик ранее изучал всю теорию Go, хочет освежить знания; не хватает практики, особенно по concurrency. Это не проверенная оценка знаний.
- Сверка исходных знаний выполнена по самоотчёту. Алгоритмы остаются отложенными согласно предыдущей записи.
- Текущий этап: recall, вопрос 1; ответ ожидается.
- Условие: Go-программа запустила две goroutine, каждая выполняет длительные вычисления без I/O. GOMAXPROCS=1. Можно ли назвать их выполнение конкурентным? Могут ли они исполнять Go-код параллельно? Объясни разницу на этой ситуации.
- Следующий шаг: проверить ответ, при ошибке разобрать конкретное заблуждение и дождаться исправления; следующий вопрос пока не выдавать.
- Последний отчёт: sessions/day-08-2026-09-08.md. Код упражнения пока не назначен. Оценки и повторения без изменений.
